## fixtures/

- Deterministic replay assets for `spine.replay` / `surfaces/verify/replay-test.sh`.
- `events/v1/`: inbound fixture prompts; `baseline/`: SHA256 of normalized expected outputs.
- `n8n/Spine_-_Mailroom_Enqueue.json`: n8n workflow template used to enqueue fixtures.
- Canonical guide: `docs/core/REPLAY_FIXTURES.md`.
