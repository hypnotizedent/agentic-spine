# SERVICE_REGISTRY.yaml - Machine-readable infrastructure truth
# Status: authoritative
# authority_state: authoritative
# authority_concern: service_registry_surfaces
# Last verified: 2026-03-08
# Updated: 2026-03-08
#
# Host IPs: Canonical source is DEVICE_IDENTITY_SSOT.md
#           This registry defines WHAT services exist and WHERE they run.
# Legacy service inventories under $LEGACY_ROOT are historical
# reference only and are non-authoritative for runtime operations.
#
# Scope policy:
#   IN SCOPE:  Shop-hosted services on active authority hosts (purpose-built VMs).
#              Legacy docker-host entries are preserved only as deprecated hold
#              surfaces and are not authoritative for fresh-slate Mint runtime,
#              retained docs, or active operator routing.
#   OUT OF SCOPE: Home-site services (proxmox-home, pihole-home, download-home,
#              hass) except for the canonical home media runtime.
#              media-home (VM 106) is explicitly in scope because it is the
#              authoritative live media surface during the current recovery.
#   Decision: LOOP-HOME-SERVICE-REGISTRY-SCOPE-DECISION-20260210 (2026-02-10).
#
# External ownership:
#   Canonical registry lives in spine; config files live in workbench.
#   This registry defines WHAT services exist and WHERE they run.
#   Compose files and deployment configs are workbench-owned.
#
# Internal dependency policy:
#   Internal-only containers (no published ports, no user traffic) are implicitly
#   covered by their parent stack entry. Examples: authentik-worker, authentik-postgres,
#   authentik-redis (part of caddy-auth stack), infisical-db, infisical-redis (part of
#   secrets stack). These do NOT need individual SERVICE_REGISTRY entries.
#
# Status values:
#   active      — running, serving traffic
#   deprecated  — marked for decommission, may still be running with dependents
#   parked      — intentionally stopped (operator decision, not a failure)
#   stopped     — crashed or exited unexpectedly, no health monitoring

source_repo: workbench
source_root: "~/code/workbench"

services:
  mint-os-api:
    host: docker-host
    port: 3335
    health: /health
    container: mint-os-dashboard-api
    compose: ~/stacks/mint-os/docker-compose.yml
    status: deprecated
    notes: "DEPRECATED legacy hold API on docker-host. Health probe disabled. Legacy secret authority retired; active secret authority is Spine-native infrastructure plus /spine/services/*."

  mint-os-postgres:
    host: docker-host
    port: 15432
    container: mint-os-postgres
    compose: ~/stacks/mint-os/docker-compose.yml
    status: deprecated
    notes: "DEPRECATED legacy data hold on docker-host. No fresh-slate Mint runtime depends on this database; preserve for forensic/rollback reads only until final retirement."

  # ─── Mint OS Frontends (docker-host) — legacy hold only ─────────────
  mint-os-admin:
    host: docker-host
    port: 3333
    container: mint-os-admin
    compose: ~/stacks/mint-os/docker-compose.frontends.yml
    status: deprecated
    notes: "DEPRECATED legacy hold frontend on docker-host. Not a canonical Mint operator surface; fresh-slate Mint authority is mint-apps + mint-data."

  mint-os-customer:
    host: docker-host
    port: 3334
    container: mint-os-customer
    compose: ~/stacks/mint-os/docker-compose.frontends.yml
    status: deprecated
    notes: "DEPRECATED legacy hold frontend on docker-host. Not canonical for customer-facing Mint flow."

  mint-os-production:
    host: docker-host
    port: 3336
    container: mint-os-production
    compose: ~/stacks/mint-os/docker-compose.frontends.yml
    status: deprecated
    notes: "DEPRECATED legacy hold frontend on docker-host. Fresh-slate production flow authority lives on mint-apps + finance-stack."

  mint-os-kanban:
    host: docker-host
    port: 3337
    container: mint-os-kanban
    compose: ~/stacks/mint-os/docker-compose.frontends.yml
    status: deprecated
    notes: "DEPRECATED legacy hold frontend on docker-host. Not a canonical Mint workflow surface."

  mint-os-job-estimator:
    host: docker-host
    port: 3001
    container: mint-os-job-estimator
    compose: ~/stacks/mint-os/docker-compose.frontends.yml
    status: retired
    notes: "Retired 2026-02-22. Route cut over to pricing@mint-apps:3700. Container stopped."

  mint-os-redis:
    host: docker-host
    port: 16379
    container: mint-os-redis
    compose: ~/stacks/mint-os/docker-compose.yml
    status: deprecated
    notes: "DEPRECATED redis backing the docker-host legacy hold stack only. Not authoritative for active Mint runtime."

  minio:
    host: docker-host
    port: 9000
    console_port: 9001
    health: /minio/health/live
    compose: ~/stacks/storage/docker-compose.yml
    volume: /mnt/docker/mint-os-data/minio
    networks: [storage-network, mint-os-network]
    status: deprecated
    notes: >
      DEPRECATED legacy MinIO hold on docker-host. Active Mint MinIO authority is
      mint-modules-minio on mint-data VM 212, and active Mint usage is artwork-only.
      Invoice PDFs are not active MinIO truth; retained-doc authority is Paperless.

  mint-modules-postgres:
    host: mint-data
    port: 5432
    container: mint-modules-postgres
    compose: /opt/stacks/mint-data/docker-compose.yml
    status: active
    notes: "Mint data plane PostgreSQL on VM 212 (fresh-slate)."

  mint-modules-minio:
    host: mint-data
    port: 9000
    console_port: 9001
    health: /minio/health/live
    container: mint-modules-minio
    compose: /opt/stacks/mint-data/docker-compose.yml
    status: active
    notes: "Mint data plane MinIO on VM 212 (fresh-slate). Canonical active Mint usage is artwork-only; retained invoices belong in Paperless."

  mint-modules-redis:
    host: mint-data
    port: 6379
    container: mint-modules-redis
    compose: /opt/stacks/mint-data/docker-compose.yml
    status: active
    notes: "Mint data plane Redis on VM 212 (fresh-slate)."

  cloudflared:
    host: infra-core
    compose: /opt/stacks/cloudflared/docker-compose.yml
    container: homelab-cloudflared
    notes: "Cloudflare tunnel connector (homelab-tunnel)"

  pihole:
    host: infra-core
    port: 8053
    dns_port: 53
    health: /admin
    compose: /opt/stacks/pihole/docker-compose.yml
    container: pihole
    notes: "Network-wide DNS ad-blocking. Web UI moved to 8053 (Caddy owns 80)."

  infisical:
    host: infra-core
    port: 8088
    health: /api/status
    compose: /opt/stacks/secrets/docker-compose.yml
    container: infisical
    secrets_project: infrastructure
    notes: "Secrets manager (relocated from docker-host)"

  vaultwarden:
    host: infra-core
    port: 8081
    health: /alive
    compose: /opt/stacks/vaultwarden/docker-compose.yml
    container: vaultwarden
    notes: "Password manager (relocated from proxmox-home VM 102, soak)"

  caddy:
    host: infra-core
    port: 80
    container: caddy
    compose: /opt/stacks/caddy-auth/docker-compose.yml
    notes: "Reverse proxy (HTTP only — CF tunnel handles public TLS)"

  authentik:
    host: infra-core
    port: 9000
    health: /if/flow/initial-setup/
    container: authentik-server
    compose: /opt/stacks/caddy-auth/docker-compose.yml
    secrets_project: infrastructure
    public_url: https://auth.ronny.works
    notes: "SSO/Authentication. Initial setup required at first login."

  # ─── Dashy (observability VM 205) ────────────────────────────────
  dashy:
    host: observability
    port: 4000
    health: /
    compose: /opt/stacks/dashy/docker-compose.yml
    container: dashy
    status: active
    public_url: https://dash.ronny.works
    notes: "Visual runtime map of all spine-managed services. Lissy93/dashy, CF tunnel routed. Migrated from docker-host (LOOP-OBSERVABILITY-DOMAIN-ENRICHMENT-20260226)."

  # ─── Mint Modules (mint-apps VM 213 runtime plane) ──
  files-api:
    host: mint-apps
    port: 3500
    health: /health
    source: mint-modules/artwork
    compose: /opt/stacks/mint-apps/docker-compose.yml
    container: files-api
    networks: [mint-modules]
    status: active
    notes: "Artwork module API on mint-apps app plane. Data dependencies route to mint-data VM."

  quote-page:
    host: mint-apps
    port: 3341
    health: /health
    source: mint-modules/quote-page
    compose: /opt/stacks/mint-apps/docker-compose.yml
    container: quote-page
    networks: [mint-modules]
    status: active
    public_url: https://mintprints.com/quote
    notes: "Quote intake service routed to mint-apps runtime at the canonical public quote path."

  order-intake:
    host: mint-apps
    port: 3400
    health: /health
    source: mint-modules/order-intake
    compose: /opt/stacks/mint-apps/docker-compose.yml
    container: order-intake
    networks: [mint-modules]
    status: active
    notes: "Order intake API hosted on mint-apps runtime."

  finance-adapter:
    host: mint-apps
    port: 3600
    health: /health
    source: mint-modules/finance-adapter
    compose: /opt/stacks/mint-apps/docker-compose.yml
    container: finance-adapter
    networks: [mint-modules]
    status: active
    notes: "Finance adapter API hosted on mint-apps runtime."

  pricing:
    host: mint-apps
    port: 3700
    health: /health
    source: mint-modules/pricing
    compose: /opt/stacks/mint-apps/docker-compose.yml
    container: pricing
    networks: [mint-modules]
    status: active
    public_url: https://pricing.mintprints.com
    notes: "Pricing module runtime on mint-apps. Canonical public work URL moved to mintprints.com."

  suppliers:
    host: mint-apps
    port: 3800
    health: /health
    source: mint-modules/suppliers
    compose: /opt/stacks/mint-apps/docker-compose.yml
    container: suppliers
    networks: [mint-modules]
    status: active
    notes: "Supplier provider orchestration module runtime on mint-apps."

  shipping:
    host: mint-apps
    port: 3900
    health: /health
    source: mint-modules/shipping
    compose: /opt/stacks/mint-apps/docker-compose.yml
    container: shipping
    networks: [mint-modules]
    status: active
    notes: "Shipping module runtime on mint-apps."

  payment:
    host: mint-apps
    port: 4000
    health: /health
    source: mint-modules/payment
    compose: /opt/stacks/mint-apps/docker-compose.yml
    container: payment
    networks: [mint-modules]
    status: active
    notes: "Payment module runtime on mint-apps."

  shopify-module:
    host: mint-apps
    port: 4100
    health: /health
    source: mint-modules/shopify-module
    compose: /opt/stacks/mint-apps/docker-compose.yml
    container: shopify-module
    networks: [mint-modules]
    status: active
    notes: "Shopify integration module runtime on mint-apps."

  digital-proofs:
    host: mint-apps
    port: 4200
    health: /health
    source: mint-modules/digital-proofs
    compose: /opt/stacks/mint-apps/docker-compose.yml
    container: digital-proofs
    networks: [mint-modules]
    status: parked
    notes: "Digital proofs MVP complete (local dev functional, deployment not yet scheduled)"

  # ─── Automation Stack (VM 202) ────────────────────────────────────
  n8n:
    host: automation-stack
    port: 5678
    health: /healthz
    compose: /home/automation/stacks/automation/docker-compose.yml
    container: n8n
    public_url: https://n8n.ronny.works
    notes: "Workflow automation (VM 202)."

  open-webui:
    host: automation-stack
    port: 3000
    health: /
    compose: /home/automation/stacks/automation/docker-compose.yml
    container: open-webui
    notes: "Web UI for Ollama (VM 202). Host port 3000 -> container 8080."

  ollama:
    host: automation-stack
    port: 11434
    compose: /home/automation/stacks/automation/docker-compose.yml
    container: ollama
    notes: "Local model runtime (VM 202)."

  automation-postgres:
    host: automation-stack
    port: 5432
    compose: /home/automation/stacks/automation/docker-compose.yml
    container: automation-postgres
    notes: "Postgres for n8n (internal-only; not exposed on host)."

  automation-redis:
    host: automation-stack
    port: 6379
    compose: /home/automation/stacks/automation/docker-compose.yml
    container: automation-redis
    notes: "Redis for automation stack (internal-only; not exposed on host)."

  mcpjungle:
    host: automation-stack
    port: 8090
    health: /health
    compose: ~/stacks/mcpjungle/docker-compose.yml
    container: mcpjungle
    notes: "MCP gateway/runtime (migrated off docker-host to automation-stack tooling plane)."

  frigate:
    host: surveillance-stack
    port: 5000
    health: /api/version
    compose: /opt/stacks/surveillance/docker-compose.yml
    container: frigate
    status: active
    notes: "Shop surveillance runtime on VM 215. CPU-first Frigate baseline with 8 live cameras. Home Assistant uses direct Tailscale URL http://100.89.1.111:5000 and MQTT at 100.67.120.1:1883."

  go2rtc:
    host: surveillance-stack
    port: 5000
    health: /api/go2rtc/api/streams
    compose: /opt/stacks/surveillance/docker-compose.yml
    container: frigate
    status: active
    notes: "Low-latency RTSP/WebRTC proxy bundled inside Frigate on surveillance-stack VM 215 and exposed through the Frigate API path. No standalone go2rtc container or socat relay is canonical."

  prometheus:
    host: observability
    port: 9090
    health: /-/healthy
    compose: /opt/stacks/prometheus/docker-compose.yml
    container: prometheus
    notes: "Metrics collection and alerting"

  grafana:
    host: observability
    port: 3000
    health: /api/health
    compose: /opt/stacks/grafana/docker-compose.yml
    container: grafana
    notes: "Monitoring dashboards and visualization"

  loki:
    host: observability
    port: 3100
    health: /ready
    compose: /opt/stacks/loki/docker-compose.yml
    container: loki
    notes: "Log aggregation"

  uptime-kuma:
    host: observability
    port: 3001
    health: /
    compose: /opt/stacks/uptime-kuma/docker-compose.yml
    container: uptime-kuma
    notes: "Uptime monitoring"

  node-exporter:
    host: observability
    port: 9100
    health: /metrics
    compose: /opt/stacks/node-exporter/docker-compose.yml
    container: node-exporter
    notes: "Host metrics exporter for Prometheus"

  gitea:
    host: dev-tools
    port: 3000
    ssh_port: 2222
    health: /api/healthz
    compose: /opt/stacks/gitea/docker-compose.yml
    container: gitea
    secrets_project: infrastructure
    public_url: https://git.ronny.works
    notes: "Git forge (dev-tools VM 206), Authentik SSO"

  gitea-runner:
    host: dev-tools
    container: gitea-runner
    compose: /opt/stacks/gitea/docker-compose.yml
    notes: "Gitea Actions CI runner (Docker-in-Docker)"

  gitea-postgres:
    host: dev-tools
    port: 5432
    container: gitea-postgres
    compose: /opt/stacks/gitea/docker-compose.yml
    notes: "PostgreSQL for Gitea"

  # ─── Media Home (VM 106) — canonical runtime, jellyfin-only recovery ─────
  radarr:
    host: media-home
    port: 7878
    health: /ping
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: radarr
    status: parked
    notes: "Canonical movie manager on media-home. Intentionally parked during jellyfin-only recovery."

  sonarr:
    host: media-home
    port: 8989
    health: /ping
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: sonarr
    status: parked
    notes: "Canonical TV manager on media-home. Intentionally parked during jellyfin-only recovery."

  lidarr:
    host: media-home
    port: 8686
    health: /ping
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: lidarr
    status: parked
    notes: "Canonical music manager on media-home. Intentionally parked during jellyfin-only recovery."

  prowlarr:
    host: media-home
    port: 9696
    health: /ping
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: prowlarr
    status: parked
    notes: "Canonical indexer manager on media-home. Intentionally parked during jellyfin-only recovery."

  sabnzbd:
    host: media-home
    port: 8080
    health: /api?mode=version&output=json
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: sabnzbd
    status: parked
    notes: "Canonical Usenet downloader on media-home. Intentionally parked during jellyfin-only recovery."

  tdarr:
    host: media-home
    port: 8265
    health: /
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: tdarr
    status: parked
    notes: "Transcode automation. Parked on media-home; not part of the active recovery surface."

  trailarr:
    host: media-home
    port: 7667
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: trailarr
    status: parked
    notes: "Canonical trailer manager on media-home. Intentionally parked during jellyfin-only recovery."

  qbittorrent:
    host: media-home
    port: 8081
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: qbittorrent
    status: parked
    notes: "Canonical BitTorrent client on media-home. Intentionally parked during jellyfin-only recovery."

  unpackerr:
    host: media-home
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: unpackerr
    status: parked
    notes: "Canonical archive extractor on media-home. Intentionally parked during jellyfin-only recovery."

  recyclarr:
    host: media-home
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: recyclarr
    status: parked
    notes: "Canonical TRaSH sync helper on media-home. Intentionally parked during jellyfin-only recovery."

  flaresolverr:
    host: media-home
    port: 8191
    health: /health
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: flaresolverr
    status: parked
    notes: "Canonical indexer challenge solver on media-home. Intentionally parked during jellyfin-only recovery."

  gluetun:
    host: media-home
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: gluetun
    status: parked
    notes: "Canonical VPN sidecar for media-home P2P services. Intentionally parked during jellyfin-only recovery."

  slskd:
    host: media-home
    port: 5030
    p2p_port: 50300
    health: /metrics
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: slskd
    status: parked
    notes: "Canonical Soulseek client on media-home. Intentionally parked during jellyfin-only recovery."

  soularr:
    host: media-home
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: soularr
    status: parked
    notes: "Canonical Soulseek integration on media-home. Intentionally parked during jellyfin-only recovery."

  swaparr-radarr:
    host: media-home
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: swaparr-radarr
    status: parked
    notes: "Deprecated swap helper retained only as a media-home alias. Parked."

  swaparr-sonarr:
    host: media-home
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: swaparr-sonarr
    status: parked
    notes: "Deprecated swap helper retained only as a media-home alias. Parked."

  swaparr-lidarr:
    host: media-home
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: swaparr-lidarr
    status: parked
    notes: "Deprecated swap helper retained only as a media-home alias. Parked."

  posterizarr:
    host: media-home
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: posterizarr
    status: parked
    notes: "Canonical poster manager on media-home. Intentionally parked during jellyfin-only recovery."

  decypharr:
    host: media-home
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: decypharr
    status: parked
    notes: "Canonical Real-Debrid integration on media-home. Intentionally parked during jellyfin-only recovery."

  autopulse:
    host: media-home
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: autopulse
    status: parked
    notes: "Canonical library refresh helper on media-home. Intentionally parked during jellyfin-only recovery."

  crosswatch:
    host: media-home
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: crosswatch
    status: parked
    notes: "Canonical watch-sync helper on media-home. Intentionally parked during jellyfin-only recovery."

  crowdsec:
    host: media-home
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: crowdsec
    status: parked
    notes: "Canonical media security sidecar on media-home. Intentionally parked during jellyfin-only recovery."

  watchtower-download:
    host: media-home
    container: watchtower
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    status: parked
    notes: "Legacy alias for the unified media-home watchtower. Intentionally parked during jellyfin-only recovery."

  download-node-exporter:
    host: media-home
    port: 9100
    health: /metrics
    container: node-exporter
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    status: parked
    notes: "Legacy alias for the unified media-home node-exporter. Intentionally parked during jellyfin-only recovery."

  jellyfin:
    host: media-home
    port: 8096
    health: /health
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: jellyfin
    status: active
    notes: "Canonical live playback surface on media-home during jellyfin-only recovery."

  navidrome:
    host: media-home
    port: 4533
    health: /ping
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: navidrome
    status: parked
    notes: "Canonical music streaming surface on media-home. Intentionally parked during jellyfin-only recovery."

  jellyseerr:
    host: media-home
    port: 5055
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: jellyseerr
    status: parked
    notes: "Canonical request manager on media-home. Intentionally parked during jellyfin-only recovery."

  bazarr:
    host: media-home
    port: 6767
    health: /ping
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: bazarr
    status: parked
    notes: "Canonical subtitle manager on media-home. Intentionally parked during jellyfin-only recovery."

  homarr:
    host: media-home
    port: 7575
    health: /
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: homarr
    status: parked
    notes: "Canonical dashboard on media-home. Intentionally parked during jellyfin-only recovery."

  wizarr:
    host: media-home
    port: 5690
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: wizarr
    status: parked
    notes: "Canonical invitation helper on media-home. Intentionally parked during jellyfin-only recovery."

  spotisub:
    host: media-home
    port: 8766
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: spotisub
    status: parked
    notes: "Canonical Spotify sync helper on media-home. Intentionally parked during jellyfin-only recovery."

  subgen:
    host: media-home
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    container: subgen
    status: parked
    notes: "Canonical subtitle-generation helper on media-home. Intentionally parked during jellyfin-only recovery."

  watchtower-streaming:
    host: media-home
    container: watchtower
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    status: parked
    notes: "Legacy alias for the unified media-home watchtower. Intentionally parked during jellyfin-only recovery."

  streaming-node-exporter:
    host: media-home
    port: 9100
    health: /metrics
    container: node-exporter
    compose: /srv/appdata/compose/media-stack/docker-compose.yml
    status: parked
    notes: "Legacy alias for the unified media-home node-exporter. Intentionally parked during jellyfin-only recovery."

  # ─── Immich (VM 203) ────────────────────────────────────────────────
  immich-server:
    host: immich
    port: 2283
    health: /api/server/ping
    compose: ~/immich/docker-compose.yml
    container: immich_server
    secrets_project: immich
    notes: "Photo management server (VM 203). 135K+ assets, 4 users, 3TB library."

  immich-ml:
    host: immich
    compose: ~/immich/docker-compose.yml
    container: immich_machine_learning
    notes: "Immich ML worker (face recognition, CLIP, OCR). No external port."

  immich-postgres:
    host: immich
    port: 5432
    container: immich_postgres
    compose: ~/immich/docker-compose.yml
    notes: "PostgreSQL for Immich. Daily pg_dump at 03:00, 7-day retention."

  immich-redis:
    host: immich
    container: immich_redis
    compose: ~/immich/docker-compose.yml
    notes: "Redis cache for Immich (ephemeral, regenerated on restart)."

  # ─── Finance Stack (VM 211 — LIVE) ─────────────────
  firefly-iii-vm211:
    host: finance-stack
    port: 8080
    health: /api/v1/about
    compose: /opt/stacks/finance/docker-compose.yml
    container: firefly-iii
    status: active
    public_url: https://finances.mintprints.com
    notes: "Firefly III on finance-stack VM 211. Live — migrated from docker-host (LOOP-FINANCE-VM-SEPARATION-20260211). Canonical public finance URL moved under mintprints.com."

  firefly-importer-vm211:
    host: finance-stack
    port: 8091
    compose: /opt/stacks/finance/docker-compose.yml
    container: firefly-importer
    status: active
    notes: "Firefly III data importer on finance-stack VM 211."

  firefly-cron-vm211:
    host: finance-stack
    compose: /opt/stacks/finance/docker-compose.yml
    container: firefly-cron
    status: active
    notes: "Firefly III cron job on finance-stack VM 211."

  finance-postgres-vm211:
    host: finance-stack
    port: 5432
    compose: /opt/stacks/finance/docker-compose.yml
    container: firefly-postgres
    status: active
    notes: "PostgreSQL 16 for Firefly III, Ghostfolio, and Paperless on finance-stack VM 211."

  finance-redis-vm211:
    host: finance-stack
    port: 6379
    compose: /opt/stacks/finance/docker-compose.yml
    container: firefly-redis
    status: active
    notes: "Redis 7 for Firefly III on finance-stack VM 211."

  paperless-ngx-vm211:
    host: finance-stack
    port: 8000
    health: /api/
    compose: /opt/stacks/finance/docker-compose.yml
    container: paperless-ngx
    status: active
    public_url: https://docs.mintprints.com
    notes: "Paperless-ngx document management on finance-stack VM 211. Live — migrated from docker-host. Canonical public finance URL moved under mintprints.com."

  ghostfolio-vm211:
    host: finance-stack
    port: 3340
    compose: /opt/stacks/finance/docker-compose.yml
    container: ghostfolio
    status: active
    public_url: https://investments.mintprints.com
    notes: "Ghostfolio investment tracking on finance-stack VM 211. Live — migrated from docker-host. Canonical public finance URL moved under mintprints.com."

  vouchervault:
    host: finance-stack
    port: 8092
    health: /en/ping/
    compose: /opt/stacks/vouchervault/docker-compose.yml
    container: vouchervault
    status: active
    notes: "VoucherVault gift card manager on finance-stack VM 211. Private Tailscale-only runtime."

  mail-archiver-vm214:
    host: communications-stack
    port: 5100
    health: /
    compose: /opt/stacks/communications-stack/docker-compose.yml
    container: mail-archiver
    status: active
    notes: "Mail Archiver on communications-stack VM 214. Migrated from finance-stack VM 211 with isolated Postgres 17 DB (mail-archiver-db)."

  mail-archiver-db-vm214:
    host: communications-stack
    port: null
    compose: /opt/stacks/communications-stack/docker-compose.yml
    container: mail-archiver-db
    status: active
    notes: "Isolated PostgreSQL 17 for Mail Archiver on communications-stack VM 214. Separate from shared finance postgres."

  # ─── Communications Stack (VM 214) ──────────────────────────────────
  stalwart-mail:
    host: communications-stack
    port: 8080
    health: /
    compose: /opt/stacks/communications-stack/docker-compose.yml
    container: stalwart-mail
    status: active
    notes: "Stalwart mail server on VM 214. IMAPS 993, SMTPS 465, STARTTLS 587, admin HTTPS 8443. TLS via Let's Encrypt DNS-01 (Cloudflare)."

  # ─── Other ──────────────────────────────────────────────────────────
  anythingllm:
    host: ai-consolidation
    port: 3002
    health: /api/ping
    compose: /opt/stacks/ai-consolidation/docker-compose.yml
    container: ai-consolidation-anythingllm-1
    notes: "Migrated from macbook (LOOP-AI-CONSOLIDATION-20260208)"

  qdrant:
    host: ai-consolidation
    port: 6333
    health: /healthz
    compose: /opt/stacks/ai-consolidation/docker-compose.yml
    container: ai-consolidation-qdrant-1
    notes: "Migrated from macbook (LOOP-AI-CONSOLIDATION-20260208)"

hosts:
  docker-host:
    tailscale_ip: 100.92.156.118
    ssh: docker-host
    os: linux-mint
    notes: "Legacy hold VM 200 only. Not authoritative for fresh-slate Mint runtime, finance, or retained-doc workflows."

  automation-stack:
    tailscale_ip: 100.98.70.70
    ssh: automation-stack
    os: ubuntu-24.04
    vmid: 202
    proxmox_host: pve

  infra-core:
    tailscale_ip: 100.92.91.128
    ssh: infra-core
    os: ubuntu-24.04
    vmid: 204
    proxmox_host: pve

  proxmox-home:
    tailscale_ip: 100.103.99.62
    ssh: proxmox-home
    os: debian-12
    notes: "Beelink Mini - home hypervisor"

  observability:
    tailscale_ip: 100.120.163.70
    ssh: observability
    os: ubuntu-24.04
    vmid: 205
    proxmox_host: pve

  dev-tools:
    tailscale_ip: 100.90.167.39
    ssh: dev-tools
    os: ubuntu-24.04
    vmid: 206
    proxmox_host: pve

  immich:
    tailscale_ip: 100.114.101.50
    ssh: immich
    ssh_user: ubuntu
    os: ubuntu-24.04
    vmid: 203
    proxmox_host: pve
    notes: "Photo management VM. Ubuntu SSH user standardized for agent access. GAP-OP-094 network fixed."

  ai-consolidation:
    tailscale_ip: 100.71.17.29
    ssh: ai-consolidation
    os: ubuntu-24.04
    vmid: 207
    proxmox_host: pve

  download-stack:
    tailscale_ip: 100.107.36.76
    ssh: download-stack
    os: ubuntu-24.04
    vmid: 209
    proxmox_host: pve
    notes: "Legacy shop media write VM. Rollback-only and non-authoritative after media-home cutover."

  streaming-stack:
    tailscale_ip: 100.123.207.64
    ssh: streaming-stack
    os: ubuntu-24.04
    vmid: 210
    proxmox_host: pve
    notes: "Legacy shop media playback VM. Rollback-only and non-authoritative after media-home cutover."

  media-home:
    tailscale_ip: 100.113.72.41
    ssh: media-home
    os: ubuntu-24.04
    vmid: 106
    proxmox_host: proxmox-home
    notes: "Canonical unified home media runtime. Current posture is jellyfin-only recovery on /srv/appdata/compose/media-stack."

  finance-stack:
    tailscale_ip: 100.76.153.100
    ssh: finance-stack
    os: ubuntu-24.04
    vmid: 211
    proxmox_host: pve
    notes: "Finance VM (Firefly III, Paperless-ngx, Ghostfolio, Mail Archiver). Live — post cutover (LOOP-FINANCE-VM-SEPARATION-20260211)."

  mint-data:
    tailscale_ip: 100.106.72.25
    ssh: mint-data
    os: ubuntu-24.04
    vmid: 212
    proxmox_host: pve
    notes: "Mint fresh-slate data plane (PostgreSQL 16 + MinIO + Redis). LOOP-MINT-FRESH-SLATE-INFRA-BOOTSTRAP-20260212."

  mint-apps:
    tailscale_ip: 100.79.183.14
    ssh: mint-apps
    os: ubuntu-24.04
    vmid: 213
    proxmox_host: pve
    notes: "Mint fresh-slate app plane (artwork, quote-page, order-intake). LOOP-MINT-FRESH-SLATE-INFRA-BOOTSTRAP-20260212."

  communications-stack:
    # IP source: DEVICE_IDENTITY_SSOT.md (canonical for host/IPs)
    tailscale_ip: 100.115.16.37
    ssh: communications-stack
    os: ubuntu-24.04
    vmid: 214
    proxmox_host: pve
    notes: "Communications stack (Stalwart mail server). IMAPS 993, SMTPS 465, STARTTLS 587, HTTPS admin 8443. TLS via Let's Encrypt DNS-01 (Cloudflare). Canonical LAN IP 192.168.1.26 is now static."

  surveillance-stack:
    # IP source: DEVICE_IDENTITY_SSOT.md (canonical for host/IPs)
    tailscale_ip: 100.89.1.111
    ssh: surveillance-stack
    os: ubuntu-24.04
    vmid: 215
    proxmox_host: pve
    notes: "Surveillance stack (Frigate + bundled go2rtc). Internal-only by design. CPU-first baseline launched 2026-03-05 on VM 215. HA integration is direct over Tailscale with no proxy relay."

  macbook:
    # IP source: DEVICE_IDENTITY_SSOT.md (canonical for host/IPs)
    tailscale_ip: 100.85.186.7
    ssh: localhost
    os: macos

infisical_projects:
  mint-os-api: 6c67b03e-ed17-4154-9a94-59837738e432
  infrastructure: 01ddd93a-e0f8-4c7c-ad9f-903d76ef94d9
