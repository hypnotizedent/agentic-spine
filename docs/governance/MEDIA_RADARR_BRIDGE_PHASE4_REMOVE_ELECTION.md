# Media Radarr Bridge Phase 4 (Remove) — Election

## Result
Authorized — operator-approved destructive bridge tranche.

## Implementation Disposition
Authorized. Preview mode default, `--apply` for live deletion. Import exclusion stays at product default (true).

## Next Action
Implement `remove_movie` tool in workbench and `media.radarr.remove` bridge in spine.
