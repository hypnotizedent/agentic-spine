change_id: DCR-20260224-example
status: draft
domain: example.com
business_zone: mintprints
operation_type: dns_cutover
owner: "@ronny"
risk_class: medium
blast_radius: single-domain

prechecks:
  - canonical_domain_confirmed
  - source_zone_export_captured
  - destination_zone_import_validated
  - ttl_reduction_window_completed
  - mail_records_parity_verified
  - cutover_window_approved

execution_steps:
  - Stage destination DNS records to match source zone.
  - Verify destination records against expected values.
  - Schedule cutover window with owner approval.
  - Execute cutover according to approved sequence.

postchecks:
  - authoritative_ns_match_destination
  - critical_records_resolve_correctly
  - dns_diff_against_plan_clean

rollback_steps:
  - Restore previous nameservers from registrar snapshot.
  - Re-apply previous DNS export if destination zone diverged.
  - Re-run postcheck health probes against known-good state.

success_criteria:
  - DNS resolution matches expected destination targets for apex/www/service hosts.
  - No mail-flow degradation observed during soak window.
  - All postchecks pass with zero critical findings.

notes: |
  Replace this template data with real change context.
