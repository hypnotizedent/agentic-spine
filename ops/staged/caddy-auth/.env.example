# Required secrets (store in Infisical project: infrastructure, path: /spine/vm-infra/caddy-auth)
AUTHENTIK_SECRET_KEY=replace-with-openssl-rand-base64-32
AUTHENTIK_DB_PASSWORD=replace-with-strong-password

# Routing/domain settings (only services proxied through Caddy)
AUTHENTIK_HOSTNAME=auth.ronny.works
PIHOLE_HOSTNAME=pihole.ronny.works
VAULT_HOSTNAME=vault.ronny.works
SECRETS_HOSTNAME=secrets.ronny.works

# Dead upstream variables removed 2026-02-12 (LOOP-CLOUDFLARE-INGRESS-PARITY-REMEDIATION-20260212):
# - FINANCES_*, INVESTMENTS_*, DOCS_*, MAIL_ARCHIVE_* (finance → VM 211 direct via extra_hosts)
# - CHAT_*, GRAFANA_*, MINIO_*, N8N_* (native-auth services → direct Tailscale/extra_hosts)
# All 8 services route directly from cloudflared to their backends, bypassing Caddy.
