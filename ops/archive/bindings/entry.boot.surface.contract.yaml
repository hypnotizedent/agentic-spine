version: "1.0"
updated: "2026-02-28"
owner: "@ronny"
scope: entry-boot-surface-projection
status: authoritative

generated_file: docs/governance/generated/BOOT_ENTRY_SURFACE.md

startup_block:
  marker_start: "<!-- SPINE_STARTUP_BLOCK -->"
  marker_end: "<!-- /SPINE_STARTUP_BLOCK -->"
  heading: "## Mandatory Startup Block"
  shell: bash
  commands:
    - cd ~/code/agentic-spine
    - ./bin/ops cap run session.start

post_work_verify:
  heading: "## Post-Work Verify"
  shell: bash
  commands:
    - ./bin/ops cap run verify.run -- fast
    - ./bin/ops cap run verify.run -- domain <domain>

release_certification:
  heading: "## Release Certification"
  shell: bash
  commands:
    - ./bin/ops cap run verify.release.run

surfaces:
  - AGENTS.md
  - CLAUDE.md
