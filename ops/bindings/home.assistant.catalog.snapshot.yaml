catalog: HOME_ASSISTANT_CATALOG_SNAPSHOT_V1
version: "1.0"
status: pending
created: "2026-03-02"
authority: HOME_AUTHORITY_CONTRACT_V1
ha_host: "10.0.0.100"
ha_port: 8123
note: "Populate on-site during LOOP-HOME-CANONICAL-REALIGNMENT W3"

integrations: []
  # To be populated from HA /api/config/config_entries/flow
  # Expected: Z2M, HACS, various cloud/local integrations
  # Format: { domain, title, source, state, version }

hacs_components: []
  # To be populated from HACS inventory
  # Format: { category, name, version, repository }

addons: []
  # To be populated from HA Supervisor /api/supervisor/addons
  # Known: Zigbee2MQTT
  # Format: { slug, name, version, state, auto_update }

entity_summary:
  total: TBD
  by_domain: {}
  unexpected_unavailable: TBD
  note: "D115 threshold: unexpected_unavailable < 50. Last measured: 199."
