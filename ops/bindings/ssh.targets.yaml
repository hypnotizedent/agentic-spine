# SSH Targets Binding
# Non-secret. Spine-native. No monolith runtime dependency.
# Status: authoritative for access paths only
# Last verified: 2026-04-16
#
# This surface is not physical-machine identity, admission, placement, role, or
# recovery authority. Physical machines named here must resolve through
# node.admission.status and node.recovery.status. This file supplies subordinate
# access evidence/projection only.
#
# Access policy: LAN-first for same-site (shop) targets.
# Tailscale reserved for cross-site paths (home, NAS).
# `host` is the preferred operational IP (what scripts resolve via yq .host).
# `tailscale_ip` is the Tailscale CGNAT IP for cross-site or fallback use.
# Human-facing workbench SSH config is generated from this binding and may project
# lan_first targets to Tailscale IPs for location-agnostic operator access.
#
# Remote Probe Resolution Policy (D321):
#   All diagnostic/status capabilities that SSH or HTTP-probe remote hosts MUST
#   resolve targets through ops/lib/ssh-resolve.sh (ssh_resolve_host_with_fallback
#   or ssh_resolve_url_with_fallback). Direct hardcoded LAN IP assignment is
#   prohibited. Unmapped targets (no SSH target entry) are passed through unchanged
#   and flagged as "unmapped" in output. Gate D321 enforces this invariant.

status: authoritative
ssh:
  defaults:
    user: "root"
    port: 22
    connect_timeout_sec: 5
    batch_mode: true
    # Avoid HOME drift (no known_hosts writes):
    strict_host_key_checking: "no"
    user_known_hosts_file: "/dev/null"
    # Machine auth-origin authority (D-SSH-IDENTITY):
    # Controls whether machine SSH surfaces use a governed identity or ambient discovery.
    # Values: explicit (governed key, IdentitiesOnly=yes), ambient (local agent/discovery)
    machine_auth_origin: "explicit"
    # Default identity file for machine SSH execution.
    # Only used when machine_auth_origin is explicit.
    machine_identity_file: "~/.ssh/spine_machine_ed25519"

  targets:
    # ─── Shop VMs (LAN-first: host=lan_ip) ───

    - id: "docker-host"
      host: "192.168.1.200"
      tailscale_ip: "100.92.156.118"
      user: "docker-host"
      optional: true
      access_policy: tailscale_required
      description: "Legacy docker-host VM 200. Tombstoned 2026-03-06; historical identity only for controlled restore/extraction work and excluded from fresh-slate reachability green."
      tags: ["legacy", "forensic", "tombstone"]

    - id: "automation-stack"
      host: "192.168.1.110"
      tailscale_ip: "100.98.70.70"
      user: "automation"
      access_policy: lan_first
      description: "n8n, Ollama, Open WebUI (VM 202). Canonical compose owner user."
      tags: ["automation", "ai", "shop"]

    - id: "immich"
      host: "192.168.1.203"
      tailscale_ip: "100.114.101.50"
      user: "ronny"
      access_policy: lan_first
      aliases: ["immich-1"]
      description: "Immich photo management (VM 203). Cloud-init user is ronny."
      tags: ["photos", "docker", "shop"]

    - id: "infra-core"
      host: "192.168.1.204"
      tailscale_ip: "100.92.91.128"
      user: "ubuntu"
      access_policy: lan_first
      description: "Core infra VM (relocation target, VM 204)"
      tags: ["infrastructure", "core", "shop", "secrets", "security"]

    - id: "observability"
      host: "192.168.1.217"
      tailscale_ip: "100.123.152.45"
      user: "ubuntu"
      access_policy: lan_first
      probe_via: pve-r620
      machine_auth_origin: explicit
      machine_identity_file: "~/.ssh/id_ed25519"
      description: "Canonical observability witness VM 216 on pve-r620."
      tags: ["observability", "monitoring", "shop", "r620", "canonical"]

    - id: "observability-legacy"
      host: "192.168.1.205"
      tailscale_ip: "100.120.163.70"
      user: "ubuntu"
      access_policy: lan_first
      description: "Legacy observability VM 205; retirement hold after VM 216 promotion."
      tags: ["observability", "monitoring", "shop", "legacy", "retirement-hold"]

    - id: "observability-r620"
      host: "192.168.1.217"
      tailscale_ip: "100.123.152.45"
      user: "ubuntu"
      access_policy: lan_first
      probe_via: pve-r620
      machine_auth_origin: explicit
      machine_identity_file: "~/.ssh/id_ed25519"
      description: "Canonical observability witness VM 216. Retained explicit target for R620-addressed operations."
      tags: ["observability", "monitoring", "shop", "r620", "canonical"]

    - id: "dev-tools"
      host: "192.168.1.206"
      tailscale_ip: "100.90.167.39"
      user: "ubuntu"
      access_policy: lan_first
      aliases: ["gitea"]
      description: "Dev tools VM (Gitea, PostgreSQL, Gitea Actions runner) - VM 206"
      tags: ["development", "ci", "shop"]

    - id: "ai-consolidation"
      host: "192.168.1.8"
      tailscale_ip: "100.71.17.29"
      user: "ubuntu"
      access_policy: lan_first
      description: "AI consolidation VM (Qdrant direct RAG runtime) - VM 207. Open WebUI stays on VM 202 (P4 decision). LAN IP is DHCP."
      tags: ["ai", "docker", "shop"]

    - id: "download-stack"
      host: "192.168.1.209"
      tailscale_ip: "100.107.36.76"
      user: "ubuntu"
      optional: true
      access_policy: lan_first
      description: "Download stack VM 209. Tombstoned 2026-03-24; 0 running containers, media workloads consolidated on media-home VM 106."
      tags: ["media", "docker", "shop", "tombstone"]

    - id: "streaming-stack"
      host: "192.168.1.210"
      tailscale_ip: "100.123.207.64"
      user: "ubuntu"
      optional: true
      access_policy: lan_first
      description: "Streaming stack VM 210. Tombstoned 2026-03-24; 0 running containers, media workloads consolidated on media-home VM 106."
      tags: ["media", "docker", "shop", "tombstone"]

    - id: "finance-stack"
      host: "192.168.1.211"
      tailscale_ip: "100.76.153.100"
      user: "ubuntu"
      access_policy: lan_first
      description: "Finance VM (Firefly III, Paperless-ngx, Ghostfolio) - VM 211. Migrated from docker-host (LOOP-FINANCE-VM-SEPARATION-20260211)."
      tags: ["finance", "docker", "shop"]

    - id: "mint-data"
      host: "192.168.1.212"
      tailscale_ip: "100.106.72.25"
      user: "ubuntu"
      access_policy: lan_first
      description: "Mint fresh-slate data plane VM 212 (PostgreSQL + MinIO + Redis)"
      tags: ["mint", "data", "docker", "shop"]

    - id: "mint-apps"
      host: "192.168.1.213"
      tailscale_ip: "100.79.183.14"
      user: "ubuntu"
      access_policy: lan_first
      description: "Mint fresh-slate app plane VM 213 (artwork, quote-page, order-intake)"
      tags: ["mint", "apps", "docker", "shop"]

    - id: "communications-stack"
      host: "192.168.1.26"
      tailscale_ip: "100.115.16.37"
      user: "ubuntu"
      access_policy: lan_first
      probe_via: pve
      description: "Communications stack VM 214 (Stalwart mail server)."
      tags: ["communications", "email", "docker", "shop"]

    - id: "surveillance-stack"
      host: "192.168.1.215"
      tailscale_ip: "100.89.1.111"
      user: "ubuntu"
      access_policy: lan_first
      description: "Surveillance stack VM 215 (Frigate/go2rtc, CPU-first)."
      tags: ["surveillance", "cameras", "docker", "shop"]

    # archive-smb (LXC 220) — RETIRED 2026-04-14
    # Samba export surface decommissioned; no live consumers since 2026-03-22.
    # md1400/archive remains canonical archive storage via direct pve access.
    # Operator cutover to direct md1400 path confirmed by empty mounts + dead launchd job.

    - id: "shop-files"
      host: "192.168.1.221"
      user: "root"
      access_policy: lan_first
      probe_via: pve
      description: "MintPrints Live L3 file server LXC 221 serving /data/shop/ via Samba on tank (hot runtime). Proved consumers: dfs-laptop (share delivered), screenpro-lenovo (share proved 2026-04-14). Unproved: Brother GTX, VersaWorks."
      tags: ["mint", "files", "shop", "lxc", "production"]

    # ─── Cross-site targets (Tailscale required) ───

    - id: "pve"
      host: "100.96.211.33"
      lan_ip: "192.168.1.184"
      tailscale_ip: "100.96.211.33"
      user: "root"
      access_policy: tailscale_required
      aliases: ["pve-shop"]
      description: "Proxmox VE - shop location. LAN IP 192.168.1.184 (from hardware.inventory.yaml). OOB recovery via idrac-shop (192.168.1.250, LAN-only, proved 2026-04-14)."
      tags: ["proxmox", "hypervisor", "core", "shop"]
      site: shop
      recovery_target_id: "idrac-shop"

    - id: "pve-r620"
      host: "100.92.252.36"
      lan_ip: "192.168.1.126"
      tailscale_ip: "100.92.252.36"
      user: "root"
      access_policy: tailscale_required
      description: "Standalone Proxmox VE - shop R620. LAN IP 192.168.1.126. OOB recovery via idrac-r620 (192.168.1.125, LAN-only, proved 2026-04-16)."
      tags: ["proxmox", "hypervisor", "shop"]
      site: shop
      recovery_target_id: "idrac-r620"

    - id: "proxmox-home"
      host: "100.103.99.62"
      tailscale_ip: "100.103.99.62"
      user: "root"
      access_policy: tailscale_required
      aliases: ["beelink", "home-backup", "offsite"]
      description: "Proxmox Host (Home) - Beelink Mini"
      tags: ["proxmox", "backup", "home"]
      site: home

    - id: "nas"
      host: "100.102.199.111"
      tailscale_ip: "100.102.199.111"
      user: "ronadmin"
      access_policy: tailscale_required
      machine_auth_origin: ambient
      aliases: ["synology918"]
      description: "Home storage node. Canonical operator/agent id is nas; Synology DS918+ and synology918 are hardware/legacy aliases only."
      tags: ["storage", "backup", "core", "home", "storage-node"]
      site: home

    - id: "pihole-home"
      host: "10.0.0.57"
      tailscale_ip: "100.114.207.32"
      user: "ubuntu"
      machine_auth_origin: explicit
      machine_identity_file: "~/.ssh/id_ed25519"
      connect_timeout_sec: 20
      access_policy: lan_first
      description: "Canonical home Pi-hole DNS appliance on Raspberry Pi 3 Model B. Supersedes proxmox-home LXC 105, which is rollback/retirement only after 2026-04-30 cutover."
      tags: ["dns", "network", "home", "raspberry-pi", "canonical"]

    - id: "media-home"
      host: "10.0.0.106"
      tailscale_ip: "100.113.72.41"
      user: "ubuntu"
      optional: true
      access_policy: lan_first
      probe_via: proxmox-home
      description: "Canonical home media/watch VM 106. LAN is preferred on-site; Tailscale is the admitted cross-site control-plane path."
      tags: ["media", "home", "vm", "active"]

    - id: "macbook-2016-pro"
      host: "10.0.0.216"
      tailscale_ip: "100.65.170.44"
      mac: "00:05:1b:8d:9c:69"
      user: "ubuntu"
      machine_auth_origin: explicit
      machine_identity_file: "~/.ssh/id_ed25519"
      optional: true
      access_policy: lan_first
      description: "Operator-owned MacBook Pro (15-inch, 2016) running Ubuntu 24.04 LTS. Boring baseline 2026-04-15. verification_node candidacy. Both SSH paths proved, reboot + DHCP lease-loss-reclaim proved. Tailscale reauth completed 2026-04-16; clean name 2016macnode at 100.65.170.44. UDR7 DHCP reservation confirmed at 10.0.0.216."
      tags: ["operator-hardware", "macbook", "linux", "home", "verification-node-candidate"]
      site: home

    - id: "raspberry-pi-home-1"
      host: "10.0.0.57"
      tailscale_ip: "100.114.207.32"
      mac: "b8:27:eb:26:52:f7"
      user: "ubuntu"
      machine_auth_origin: explicit
      machine_identity_file: "~/.ssh/id_ed25519"
      optional: true
      access_policy: lan_first
      description: "Operator-owned Raspberry Pi 3 Model B (home). Hardware identity backing canonical pihole-home DNS appliance as of 2026-04-30; keep this id for physical node readback."
      tags: ["operator-hardware", "raspberry-pi", "linux", "home", "dns-appliance"]
      site: home

    # ─── Direct Tailscale targets ───

    - id: "ha"
      host: "100.67.120.1"
      tailscale_ip: "100.67.120.1"
      user: "hassio"
      access_policy: tailscale_required
      aliases: ["homeassistant", "home-assistant"]
      description: "Home Assistant via kernel-mode Tailscale add-on. Canonical remote path is direct Tailscale; LAN UI remains 10.0.0.100."
      tags: ["home-automation", "iot", "tailscale"]

    - id: "udr-home"
      host: "10.0.0.1"
      user: "root"
      access_method: lan_only
      access_policy: lan_only
      probe_via: proxmox-home
      description: "UDR7 home gateway/router (UniFi console). LAN-only probe via proxmox-home."
      tags: ["router", "network", "home", "lan-only", "unifi", "gateway"]
      site: home

    # vault (VM 102): decommissioned 2026-02-16. Superseded by infra-core.
    # media-stack (VM 201) DECOMMISSIONED 2026-02-10 — split into download-stack (209) + streaming-stack (210)
    # LXC 103 (download-home) soft-decommissioned 2026-02-21.

    # ─── LAN-only devices (no Tailscale, checked via probe_via host) ───

    - id: "switch-shop"
      host: "192.168.1.2"
      mac: "f8:b1:56:73:a0:d0"
      user: "admin"
      access_method: lan_only
      access_policy: lan_only
      probe_via: pve
      description: "Dell N2024P managed switch — shop 9U rack. Re-IP deferred from UDR6 cutover."
      tags: ["network", "shop", "lan-only"]

    - id: "idrac-shop"
      host: "192.168.1.250"
      mac: "44:a8:42:26:c3:11"
      user: "root"
      access_method: lan_only
      access_policy: lan_only
      probe_via: pve
      recovery_access: direct_lan_only
      recovery_status: proved
      proved_at: "2026-04-14"
      description: "Dell R730XD iDRAC — shop. Recovery access is direct LAN only (192.168.1.250). Redfish API, power status, and web console proved 2026-04-14."
      tags: ["management", "shop", "lan-only", "oob", "recovery"]

    - id: "idrac-r620"
      host: "192.168.1.125"
      mac: "74:86:7a:d1:53:f8"
      user: "root"
      secret_keys:
        user: "IDRAC_R620_ADMIN_USER"
        password: "IDRAC_R620_ADMIN_PASSWORD"
      access_method: lan_only
      access_policy: lan_only
      probe_via: pve
      recovery_access: direct_lan_only
      recovery_status: proved
      proved_at: "2026-04-16"
      description: "Dell R620 iDRAC — shop. SSH+racadm, virtual media remoteimage, one-time boot control, and power control proved live on 2026-04-16. Dedicated governed credential posture uses /spine/shop/idrac-r620."
      tags: ["management", "shop", "lan-only", "oob", "recovery"]

    - id: "nvr-shop"
      host: "192.168.1.216"
      mac: "24:0f:9b:30:f1:e7"
      user: "admin"
      access_method: lan_only
      access_policy: lan_only
      probe_via: pve
      description: "Hikvision NVR — shop. Re-IP via ISAPI from pve. Credentials in Infisical /spine/shop/nvr/."
      tags: ["cameras", "shop", "lan-only"]

    - id: "ap-shop"
      host: "192.168.1.185"
      mac: "54:af:97:2f:c6:6e"
      user: "Production"
      access_method: lan_only
      access_policy: lan_only
      probe_via: pve
      ssh_extra_opts: "-o HostKeyAlgorithms=+ssh-rsa -o PubkeyAcceptedAlgorithms=+ssh-rsa"
      description: "TP-Link EAP225 WiFi AP — shop. Dropbear SSH (ssh-rsa only). Creds in Infisical /spine/shop/wifi/."
      tags: ["wifi", "network", "shop", "lan-only"]

    - id: "udr-shop"
      host: "192.168.1.1"
      mac: "9c:05:d6:be:5b:4c"
      user: "root"
      access_method: lan_only
      access_policy: lan_only
      probe_via: pve
      description: "UDR6 shop gateway/router (UniFi console). LAN-only probe via pve."
      tags: ["router", "network", "shop", "lan-only", "unifi", "gateway"]
      site: shop

    - id: "dfs-laptop"
      host: "192.168.1.137"
      mac: "74:4c:a1:d6:08:ad"
      user: "marll"
      access_method: lan_only
      access_policy: lan_only
      probe_via: pve
      description: "Windows 11 DFS laptop — serves designs to Barudan via port 8000."
      tags: ["dfs", "mint", "shop", "windows", "production"]
      site: shop

    - id: "screenpro-lenovo"
      host: "192.168.1.95"
      tailscale_ip: "100.73.70.75"
      mac: "e8:62:be:9a:db:93"
      user: "Administrator"
      access_policy: lan_first
      probe_via: pve
      description: "ScreenPRO 600 RIP workstation (Lenovo) — PrintProduction + PCS6 color management. Wi-Fi connected."
      tags: ["screenpro", "rip", "mint", "shop", "windows", "production"]
      site: shop
    - id: optiplex-9020-001
      host: 192.168.1.226
      tailscale_ip: 100.72.218.93
      user: ubuntu
      access_policy: tailscale_required
      description: Operator-owned Dell OptiPlex 9020 at shop. Ubuntu 24.04 Stage 0 firstboot claim proved 2026-04-29. Tailscale 100.72.218.93. Not role-promoted.
      tags: [operator-hardware, dell, optiplex, linux, shop, candidate]
      site: shop
    - id: optiplex-9020-002
      host: 192.168.1.118
      tailscale_ip: 100.82.8.74
      user: ubuntu
      access_policy: tailscale_required
      description: Operator-owned Dell OptiPlex 9020 at shop. Ubuntu 24.04 Stage 0 firstboot claim proved 2026-04-29. Tailscale 100.82.8.74. Not role-promoted.
      tags: [operator-hardware, dell, optiplex, linux, shop, candidate]
      site: shop
    - id: shuttle-xpc-xc60j-001
      host: 192.168.1.247
      tailscale_ip: 100.118.237.119
      user: ubuntu
      access_policy: tailscale_required
      description: Operator-owned Shuttle XPC XC60J 001 at shop. Ubuntu 24.04 Stage 0 firstboot claim proved 2026-05-01. Tailscale 100.118.237.119. Not role-promoted.
      tags: [operator-hardware, shuttle, xpc, xc60j, linux, shop, candidate]
      site: shop
