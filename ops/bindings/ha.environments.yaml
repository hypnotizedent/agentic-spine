# HA Environments Contract (SSOT)
# Status: authoritative
# Last verified: 2026-02-17
# Scope: HA environment partitioning

schema_version: "1.0"

environments:
  - id: home
    status: active
    subnet: "10.0.0.0/24"
    ha_url: "http://10.0.0.100:8123"
    area_source: "ops/bindings/ha.areas.yaml"
    registry_source: "ops/bindings/home.device.registry.yaml"

  - id: shop
    status: planned
    subnet: null
    ha_url: null
    area_source: "ops/bindings/ha.areas.yaml"
    registry_source: "ops/bindings/home.device.registry.yaml"

lifecycle_states:
  - planned
  - active
  - deprecated

reconciliation:
  identity_surfaces:
    - "ops/bindings/ha.device.map.yaml"
    - "ops/bindings/ha.ssot.baseline.yaml"
  verify_lane: "verify.domain.run home --force"
