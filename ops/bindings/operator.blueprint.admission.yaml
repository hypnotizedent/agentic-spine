---
# operator.blueprint.admission.yaml
# Authority surface for operator blueprint intake classification.
#
# This file answers: "What happened to the human steward's blueprint files?"
# Each entry classifies an ingested operator architecture file and tracks
# its disposition through the spine admission path.
#
# Source material: operator-vision batches 2026-04-13 and 2026-04-16
# Located at: $SPINE_STATE/inputs/operator-vision-20260413/
#             $SPINE_STATE/inputs/operator-vision-20260416/

status: authoritative
owner: "@human-steward"
scope: operator-blueprint-admission
created: "2026-04-27"

# Vocabulary

operator_intent_classes:
  - blueprint          # L1/L2 platform architecture
  - domain_vision      # L3 product surface design
  - reference_only     # Intellectual framework, not directly materializable

admission_decisions:
  - materialize        # Accepted; becomes live authority/capability/readback
  - shelf              # Valuable; deferred until trigger
  - tombstone          # Superseded or rejected
  - reference_only     # Preserved as context; no materialization path

admission_statuses:
  - captured           # File exists in inputs/
  - classified         # Intent class and layer assigned
  - accepted           # Decision made, canonical home identified
  - materializing      # Active work converting to spine truth
  - proved             # Live authority/capability/readback exists
  - shelved            # Parked with trigger conditions
  - tombstoned         # Explicitly closed

# Epistemic fields (borrowed from posture contract draft, not standalone)
claim_types:
  - observed           # Direct evidence exists in spine
  - inferred           # Pattern match or indirect evidence
  - proposed           # Operator intent, not yet proved
  - accepted           # Operator-approved, spine-adopted

# --- Classification Matrix ---

entries:

  # === Batch 1: operator-vision-20260413 ===

  - id: BPA-001
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260413/Node class architecture.md"
    title: "Node Class Architecture"
    operator_intent_class: blueprint
    layer: L1
    blueprint_primitive: node_class_architecture
    decision: shelf
    status: shelved
    claim_type: proposed
    confidence: high
    freshness: fresh
    admissibility: design_authority
    canonical_home_if_materialized: ops/bindings/node.class.architecture.yaml
    summary: >
      Uses the 77-node flat taxonomy as creative vocabulary and pressure-test
      material, then reduces it into 8 planes, 10-16 durable classes, packing
      rules, and isolation policy. It is not a deployment mandate and must not
      be read as a requirement to materialize every named node.
    shelf_trigger: >
      Materialize after blueprint-to-engine admission seam is proved and
      operator intake unification is working.
    current_spine_coverage: >
      NODE_PROMOTION_LADDER (promotion stages), node.role.contract (role binding),
      fleet.admission (admission rules). All partial; no plane model, no packing
      rules, no class reduction.
    materialization_gap: >
      8-plane superstructure, class vs logical-role distinction, packing/isolation
      policy, realization patterns. The gap is the missing durable classification
      model, not unbuilt machines for the 77 creative node names.

  - id: BPA-002
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260413/Epistemology.md"
    title: "Epistemology"
    operator_intent_class: reference_only
    layer: L1
    blueprint_primitive: epistemology
    decision: reference_only
    status: classified
    claim_type: proposed
    confidence: high
    freshness: fresh
    admissibility: reference
    canonical_home_if_materialized: null
    summary: >
      Theoretical framework: Ashby's Law, stigmergy, autopoiesis, equifinality,
      hysteresis, affordances, teleological drift, second-order cybernetics,
      liminality. Explains why agentic systems fail the way they do.
    shelf_trigger: null
    current_spine_coverage: >
      Teleological drift partially addressed by stewardship-engine-vision design.
      No other concepts have spine surfaces.
    materialization_gap: >
      Not directly materializable. Serves as intellectual substrate for
      epistemic posture contract and cognitive regime governance.

  - id: BPA-003
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260413/Taxonomy pillar.md"
    title: "Taxonomy as One Pillar of Ten"
    operator_intent_class: blueprint
    layer: L1
    blueprint_primitive: taxonomy_ontology
    decision: shelf
    status: shelved
    claim_type: proposed
    confidence: high
    freshness: fresh
    admissibility: design_authority
    canonical_home_if_materialized: null
    summary: >
      Positions taxonomy as one of ten ontological pillars: identity, placement,
      contracts, realization, verification, lifecycle, dependency, failure,
      authority, economics. Argues taxonomy alone is insufficient.
    shelf_trigger: >
      Materialize after node class architecture (BPA-001) is promoted. The
      ten-pillar framing structures what comes after class reduction.
    current_spine_coverage: >
      Taxonomy (partial via node promotion + role contracts), identity (partial
      via DEVICE_IDENTITY_SSOT + ssh.identity), contracts (extensive via
      ops/bindings/), verification (verify hierarchy), lifecycle (loop/packet/wave).
      Placement, dependency, failure, economics pillars absent.
    materialization_gap: >
      No unified pillar model. Each pillar exists as ad-hoc surfaces without
      the organizing frame this file describes.

  - id: BPA-004
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260413/Preserving knowledge Claude.md"
    title: "Preserving Knowledge"
    operator_intent_class: domain_vision
    layer: L3
    blueprint_primitive: null
    decision: materialize
    status: proved
    claim_type: observed
    confidence: high
    freshness: fresh
    admissibility: action_safe
    canonical_home_if_materialized: /Users/ronnyworks/code/preserving-knowledge
    summary: >
      Islamic knowledge preservation strategy. Archive.org resources, Shamela
      database, workflow for bulk pulling Quran recitations and Islamic texts.
    materialization_target:
      authority: /Users/ronnyworks/code/preserving-knowledge
      capability: "preserving-knowledge repo product surfaces and pk-* tooling"
      proof: "preserving-knowledge README current posture, manifests, schemas, templates, and product custody roots"
    shelf_trigger: null
    current_spine_coverage: >
      The L3 product home exists outside spine at
      /Users/ronnyworks/code/preserving-knowledge. Spine retains only
      control-plane/readback responsibility; product truth belongs in the sibling
      repo. The repo carries source manifests, schemas, templates, custody-root
      posture, and product/tooling guidance.
    materialization_gap: >
      Not an absent domain. Remaining gaps are product-specific proof and corpus
      promotion inside preserving-knowledge, not a missing spine surface.

  - id: BPA-005
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260413/Node master.md"
    title: "Node Master Table (77 Nodes)"
    operator_intent_class: blueprint
    layer: L1
    blueprint_primitive: node_class_architecture
    decision: reference_only
    status: classified
    claim_type: proposed
    confidence: high
    freshness: fresh
    admissibility: reference
    canonical_home_if_materialized: null
    summary: >
      Creative table of 77 possible node functions across 6 weight classes with
      descriptions and hardware recommendations. Raw material for node class
      reduction only; not authority to build, assign, or materialize 77 nodes.
    shelf_trigger: null
    current_spine_coverage: >
      NODE_PROMOTION_LADDER carries promotion stages for real nodes.
      node.role.contract carries role bindings. The 77-node table itself is
      not in spine as deployment authority — it is the input to class reduction
      and vocabulary pressure testing (BPA-001).
    materialization_gap: >
      The table is input data, not target architecture. BPA-001 describes
      the reduction product. This file feeds BPA-001, not spine directly.
      Agents must not summarize this as "not all nodes are materialized"; the
      correct read is "creative vocabulary has been reduced into first-class
      role/readback patterns where needed."

  - id: BPA-006
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260413/The shalamat unlock.md"
    title: "Shalamat Unlock (Shamela Library)"
    operator_intent_class: domain_vision
    layer: L3
    blueprint_primitive: null
    decision: materialize
    status: materializing
    claim_type: observed
    confidence: high
    freshness: fresh
    admissibility: action_safe
    canonical_home_if_materialized: /Users/ronnyworks/code/preserving-knowledge
    summary: >
      Shamela digital Islamic library as prebuilt RAG dataset. Three-pillar
      architecture (Preservation, Verification, Activation). Phased ingestion
      with receipt-driven governance.
    materialization_target:
      authority: /Users/ronnyworks/code/preserving-knowledge
      capability: "Shamela-facing manifests, schemas, and library/readback tooling in the preserving-knowledge repo"
      proof: "preserving-knowledge README names The shalamat unlock as north-star framing and carries Shamela manifests plus verification/library schemas"
    shelf_trigger: null
    current_spine_coverage: >
      Shamela is no longer an absent idea in spine. It is a product scope inside
      /Users/ronnyworks/code/preserving-knowledge, where the README names The
      shalamat unlock as north-star framing and the repo carries Shamela
      manifests, verification schemas, library surfaces, and bounded product
      tooling.
    materialization_gap: >
      Not an absent domain. Remaining gaps are specific source/corpus proof,
      trust grading, and promoted library objects inside preserving-knowledge.

  - id: BPA-007
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260413/Topology entropy.md"
    title: "Topology, Entropy, Metabolism"
    operator_intent_class: blueprint
    layer: L1
    blueprint_primitive: system_physics
    decision: shelf
    status: shelved
    claim_type: proposed
    confidence: medium
    freshness: fresh
    admissibility: design_authority
    canonical_home_if_materialized: ops/bindings/system.physics.contract.yaml
    summary: >
      Three system models complementing existing governance: topology (spatial/
      geometric truth), entropy (decay/degradation truth), metabolism (flow/
      transformation truth).
    shelf_trigger: >
      Materialize after watcher v1 probe fidelity hardens. Watcher is the
      natural carrier for entropy/metabolism observation.
    current_spine_coverage: >
      Watcher probes (partial entropy via health classification). Node topology
      docs (partial spatial truth). No metabolism model.
    materialization_gap: >
      Explicit topology/entropy/metabolism models. Watcher carries partial
      entropy only.

  - id: BPA-008
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260413/epistemic postures.md"
    title: "Epistemic Postures"
    operator_intent_class: blueprint
    layer: L1
    blueprint_primitive: epistemology
    decision: shelf
    status: shelved
    claim_type: proposed
    confidence: high
    freshness: fresh
    admissibility: design_authority
    canonical_home_if_materialized: ops/bindings/epistemic.posture.contract.yaml
    summary: >
      Nine behavioral values: calibration, parsimony, legibility, idempotency,
      atomicity, reversibility, orthogonality, tractability, coherence. Frames
      these as absorbed culture, not enforced rules.
    shelf_trigger: >
      Materialize after blueprint admission seam proves epistemic fields work
      as borrowed intake vocabulary. If fields survive repetition, promote to
      standalone contract.
    current_spine_coverage: >
      D422 gate enforces calibration language partially. execution.posture.contract
      carries execution posture. No other postures have spine surfaces.
    materialization_gap: >
      8 of 9 postures lack spine surfaces. The draft contract (BPA-009) is the
      most promotion-ready artifact.

  - id: BPA-009
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260413/epistemic.posture.contract.draft.yaml"
    title: "Epistemic Posture Contract (Draft YAML)"
    operator_intent_class: blueprint
    layer: L1
    blueprint_primitive: epistemology
    decision: shelf
    status: shelved
    claim_type: proposed
    confidence: high
    freshness: fresh
    admissibility: design_authority
    canonical_home_if_materialized: ops/bindings/epistemic.posture.contract.yaml
    summary: >
      Machine-evaluable behavioral contract for all nine epistemic postures.
      Claim classes, evidence requirements, confidence language, enforcement
      surfaces. Near-contract form.
    shelf_trigger: >
      Promote when BPA-008 trigger is met. This is the realization artifact
      for BPA-008.
    current_spine_coverage: >
      Same as BPA-008. This file IS the draft realization.
    materialization_gap: >
      Needs promotion from draft status, landing in ops/bindings/, gate
      registration, and verify integration.

  # === Batch 2: operator-vision-20260416 ===

  - id: BPA-010
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260416/Operator vision spec.md"
    title: "Operator Vision Interface Spec"
    operator_intent_class: blueprint
    layer: L1
    blueprint_primitive: operator_intake_unification
    decision: materialize
    status: proved
    claim_type: observed
    confidence: high
    freshness: fresh
    admissibility: action_safe
    canonical_home_if_materialized: ops/bindings/operator.blueprint.admission.yaml
    summary: >
      End-to-end operator intake lifecycle: raw conversation through membrane
      translation, reconciliation, packetization, cockpit visibility. Four
      operator modes, device role clarity, six operational agents.
    materialization_target:
      authority: ops/bindings/operator.blueprint.admission.yaml
      capability: operator.ingress (existing) + blueprint admission readback
      proof: "operator.blueprint.admission.status + D443 + actor.boundary human_intent_object/D422"
    current_spine_coverage: >
      operator.ingress.submit/metabolize/reconcile (partial lifecycle).
      intake.scaffold + bundle.review (partial evidence capture). Actor
      vocabulary and human intent object (D422). Blueprint-class intake,
      classification matrix, and materialization tracking are now live.
    materialization_gap: >
      None for architecture-file admission and first-class human intent naming.
      OI remains the operational intake carrier; broader lane unification is
      intentionally handled by future bounded slices, not this matrix.

  - id: BPA-011
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260416/Operator.md"
    title: "Operator Maturity Model (Five Layers)"
    operator_intent_class: blueprint
    layer: L1
    blueprint_primitive: operator_role
    decision: reference_only
    status: classified
    claim_type: proposed
    confidence: high
    freshness: fresh
    admissibility: reference
    canonical_home_if_materialized: null
    summary: >
      Five layers of operator maturity: surface, behavior, pressure, meaning,
      trajectory. Shift from administration to stewardship. Seven protective
      practices. The founder as hidden dependency.
    shelf_trigger: null
    current_spine_coverage: >
      Stewardship-engine-vision (parked design). Translator doctrine (partial
      operator boundary). No five-layer maturity model in spine.
    materialization_gap: >
      Philosophical framework. Informs depth review (BPA-013) and operator
      intake design. Not directly materializable as a single contract.

  - id: BPA-012
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260416/Agent operator human role.md"
    title: "Agent/Operator/Human Role Spec"
    operator_intent_class: blueprint
    layer: L1
    blueprint_primitive: operator_role
    decision: materialize
    status: proved
    claim_type: observed
    confidence: high
    freshness: fresh
    admissibility: action_safe
    canonical_home_if_materialized: ops/bindings/actor.boundary.contract.yaml
    summary: >
      Canonical human role in agentic spine. Human = intent/judgment/override,
      not transport/parser/queue. Four operator lanes. Four maturity stages.
    materialization_target:
      authority: ops/bindings/actor.boundary.contract.yaml
      capability: D422 gate (actor vocabulary enforcement)
      proof: "6a1bfc53 + 5d4cfaed — actor vocabulary collapse landed"
    current_spine_coverage: >
      FULLY MATERIALIZED. actor.boundary.contract.yaml defines four
      actor terms (human_operator, agent_actor, operator_console,
      operator_ingress). D422 gate enforces regression. Actor vocabulary
      collapse landed 2026-04-27.
    materialization_gap: >
      None for core role distinction. Four operator lanes (vision intake,
      direct command, review request, approval queue) partially covered by
      OI system but not explicitly named as lanes.

  - id: BPA-013
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260416/Depth review.md"
    title: "Depth Review Framework (Five Layers)"
    operator_intent_class: blueprint
    layer: L1
    blueprint_primitive: depth_review
    decision: shelf
    status: shelved
    claim_type: proposed
    confidence: high
    freshness: fresh
    admissibility: design_authority
    canonical_home_if_materialized: ops/bindings/depth.review.contract.yaml
    summary: >
      Five-layer review framework: representation, actual behavior, operational
      meaning, architectural honesty, environmental consequence. Anti-patterns
      to catch. Extends verify beyond layers 1-2.
    shelf_trigger: >
      Materialize when verify hierarchy is next extended. Natural carrier for
      layers 3-5 that verify currently lacks.
    current_spine_coverage: >
      verify.engine.run + spine.verify cover layers 1-2 (is it alive, is it
      coherent). Temporal honesty design (proposed layer 6). No layers 3-5.
    materialization_gap: >
      Layers 3-5 (pressure/meaning/trajectory) absent from verify.

  - id: BPA-014
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260416/Regeime thinking.md"
    title: "Cognitive Regime Governance"
    operator_intent_class: blueprint
    layer: L1
    blueprint_primitive: cognitive_agent_posture
    decision: shelf
    status: shelved
    claim_type: proposed
    confidence: medium
    freshness: fresh
    admissibility: design_authority
    canonical_home_if_materialized: ops/bindings/cognitive.regime.contract.yaml
    summary: >
      Governs how agents think, not just what they execute. Cognitive regimes
      (reduction, synthesis, exploratory, adversarial, verification, planning,
      routing, mutation-safety, recovery). Deliberation budgets. Teleology
      contracts. Anti-pattern detection.
    shelf_trigger: >
      Materialize after epistemic postures (BPA-008/009) prove the behavioral
      contract pattern works. Regime governance is the next layer above postures.
    current_spine_coverage: >
      execution.posture.contract (execution posture only). No cognitive regime
      model. No deliberation budget. No anti-pattern detection.
    materialization_gap: >
      Entire cognitive regime layer absent.

  - id: BPA-015
    source_ref: "$SPINE_STATE/inputs/operator-vision-20260416/Epidemic system.md"
    source_aliases:
      - "/Users/ronnyworks/Desktop/Archive/Yes..md"
    title: "Decision Provenance / Epistemic Receipt Semantics"
    operator_intent_class: blueprint
    layer: L1
    blueprint_primitive: epistemology
    decision: shelf
    status: shelved
    claim_type: proposed
    confidence: high
    freshness: fresh
    admissibility: design_authority
    canonical_home_if_materialized: ops/bindings/epistemic.receipt.contract.yaml
    summary: >
      Classifies knowledge and makes truth explicit. Claim types, evidence
      records, source authority, epistemic class (declared/observed/verified/
      inferred/cached/imported/stale/conflicting/quarantined/revoked),
      confidence, freshness, admissibility, conflict records, decision receipts.
    shelf_trigger: >
      Materialize after blueprint admission seam proves borrowed epistemic
      fields survive repetition. This file defines the full system that the
      borrowed fields are drawn from.
    current_spine_coverage: >
      Receipt outcome vocabulary (success/failure/blocked). Verify hierarchy.
      Calibration language (D422). This admission matrix borrows claim_type,
      confidence, freshness, admissibility from this file's design.
    materialization_gap: >
      Full claim classification system, evidence typing, freshness/decay model,
      admissibility gates, conflict resolution. Currently only four fields
      borrowed for intake use.

  - id: BPA-016
    source_ref: "/Users/ronnyworks/Documents/spine notes 4.13.2026/Site hardware intelligence.md"
    source_aliases:
      - "/Users/ronnyworks/Desktop/Site hardware intelligence/Site hardware intelligence.md"
    title: "Site Hardware Intelligence"
    operator_intent_class: blueprint
    layer: L1
    blueprint_primitive: site_hardware_intelligence
    decision: shelf
    status: shelved
    claim_type: observed
    confidence: high
    freshness: fresh
    admissibility: design_authority
    canonical_home_if_materialized: ops/bindings/site.hardware.intelligence.contract.yaml
    summary: >
      Governed site-aware hardware knowledge product. Separates physical truth,
      capability truth, suitability truth, and standardization truth so owned,
      inherited, candidate, and future machines are classified before bootstrap,
      adoption, migration, retirement, or purchase decisions.
    materialization_target:
      authority: /Users/ronnyworks/code/.runtime/spine/state/domain-state/spine/SITE-HARDWARE-INTELLIGENCE-FORENSIC-20260415.md
      capability: controller packet forensic/readback, not a live product capability
      proof: "PACKET-18-SITE-HARDWARE-INTELLIGENCE + CAP-20260416-164027 + SITE-HARDWARE-INTELLIGENCE-FORENSIC-20260415"
    shelf_trigger: >
      Promote when hardware/site planning becomes the active bounded aperture.
      The existing Packet 18 forensic proved the gap and the decision model;
      the live product contract/capability remains intentionally unbuilt.
    current_spine_coverage: >
      Packet 18 delivered the forensic model: seven decision realms, eight
      required output answers, physical-vs-capability truth split, and route set
      for bootstrap/adopt/migrate/retire/physical_investigation. Hardware
      inventories and readiness capabilities remain partial inputs.
    materialization_gap: >
      No live site.hardware.intelligence capability or contract yet emits the
      uniform site-aware machine assessment object for every real or candidate
      machine.

# --- Summary Statistics ---

summary:
  total_files: 16
  blueprint_l1: 13
  domain_vision_l3: 2
  reference_only: 1
  decisions:
    materialize: 2
    shelf: 11
    reference_only: 3
    tombstone: 0
  statuses:
    proved: 2          # BPA-010: Operator Vision Spec; BPA-012: Agent/Operator/Human Role
    materializing: 0
    shelved: 11
    classified: 3
  materialization_proof:
    specimens: [BPA-010, BPA-012]
    source:
      - "Operator vision spec.md"
      - "Agent operator human role.md"
    target:
      - "ops/bindings/operator.blueprint.admission.yaml"
      - "ops/bindings/actor.boundary.contract.yaml"
    gate:
      - "D443 (blueprint admission matrix integrity)"
      - "D422 (actor vocabulary + human intent object enforcement)"
    commits: ["6a1bfc53", "5d4cfaed"]
    claim_type: observed
    evidence: "Blueprint admission readback landed; actor vocabulary and human intent object are gate-enforced."
