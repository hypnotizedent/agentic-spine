# Status: pending
# Authority: LOOP-MEDIA-SHOP-HOME-MIGRATION-CONNECTOR-20260303
# Closes: GAP-OP-1404 (when populated and enforced)
# Requires: runtime access to verify compose mounts and app configs

contract: MEDIA_PATH_AUTHORITY_CONTRACT_V1
version: "0.1"
status: pending
created: "2026-03-03"
authority_loop: LOOP-MEDIA-SHOP-HOME-MIGRATION-CONNECTOR-20260303

description: |
  Canonical path authority matrix for the media pipeline.
  Every service that touches media files must have an explicit 4-way parity
  tuple: compose_mount, app_root, downloader_path, library_path.
  Drift between any two members of a tuple is a contract violation.

# Path parity tuples — each service's file access paths must be internally consistent
# populate from live runtime (docker inspect + app API) when on-site
path_tuples:
  radarr:
    compose_mount: "/media/movies"   # docker-compose volume mount
    app_root: TBD                     # Radarr Settings > Media Management > Root Folder
    downloader_path: TBD              # Radarr Settings > Download Clients > remote path mapping
    library_path: "/media/movies"     # canonical NFS mount path
    status: pending
    gap_ref: GAP-OP-1387

  sonarr:
    compose_mount: "/media/tv"
    app_root: TBD
    downloader_path: TBD
    library_path: "/media/tv"
    status: pending

  lidarr:
    compose_mount: "/media/music"
    app_root: TBD
    downloader_path: TBD
    library_path: "/media/music"
    status: pending

  sabnzbd:
    compose_mount: TBD
    app_root: null                    # SAB has no root folder concept
    downloader_path: TBD              # SAB completed downloads path
    library_path: null
    status: pending
    gap_ref: GAP-OP-1388

  jellyfin:
    compose_mount: "/media"
    app_root: null                    # Jellyfin uses library paths directly
    downloader_path: null
    library_path: "/media/movies,/media/tv,/media/music"
    status: pending

# Enforcement
drift_gate: TBD  # future gate to enforce path parity
verification_command: TBD  # future capability to check 4-way parity

# Migration note
migration_constraint: |
  When migrating from shop to home, ALL path tuples must be re-verified
  against the new compose configuration and NFS mount points.
  See MEDIA-SHOP-HOME-MIGRATION-TRANSACTION-PACKET.md Phase 1.
