version: "1.0"
updated: "2026-02-28"
owner: "@ronny"
scope: inventory-enforcement

proxmox_vm_parity:
  gate_id: D294
  mode: report_only # report_only | enforce
  runtime_command: "qm list"
  declared_source: "ops/bindings/vm.lifecycle.yaml"
  declared_filter_status:
    - active

docker_stack_parity:
  gate_id: D295
  mode: report_only # report_only | enforce
  runtime_command: "docker compose ls --format json"
  declared_source: "ops/bindings/docker.compose.targets.yaml"
  include_legacy_only: false
