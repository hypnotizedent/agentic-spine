version: "1.0"
updated: "2026-02-28"
owner: "@ronny"
scope: verify-interface-canonicalization
status: authoritative

agent_facing:
  canonical_capability: verify.run
  canonical_examples:
    - ./bin/ops cap run verify.run -- fast
    - ./bin/ops cap run verify.run -- domain <domain>
    - ./bin/ops cap run verify.run -- release
  policy: "Agents invoke verify.run only."

internal_legacy_paths:
  allowed_for_internal_orchestration_only:
    - verify.core.run
    - verify.domain.run
    - verify.pack.run
    - verify.pack.explain
    - verify.pack.list
  do_not_recommend_to_agents: true

routing:
  recommendation_capability: verify.route.recommend
  expected_output_form:
    - ./bin/ops cap run verify.run -- fast
    - ./bin/ops cap run verify.run -- domain <domain>

acceptance:
  requires_docs_alignment:
    - AGENTS.md
    - CLAUDE.md
    - docs/governance/SESSION_PROTOCOL.md
    - ops/bindings/entry.boot.surface.contract.yaml
