version: 1
updated_at: "2026-02-25"
owner: "@ronny"
scope: mint-live-modules-before-auth

auth_module:
  eq_id: EQ-1
  state: deferred
  blocked_by_gate: D225
  unblock_requires:
    - live_health_green
    - contract_parity_green
    - agent_smoke_green

queue_contract:
  file: /Users/ronnyworks/code/mint-modules/docs/PLANNING/MINT_MODULE_EXECUTION_QUEUE.md
  required_markers:
    - BLOCKED_BY_D225
    - AUTH_DEFERRED_UNTIL_LIVE_BASELINE

prerequisites:
  live_health_green:
    capability: mint.modules.health
    pass_pattern: "^status: OK"
  contract_parity_green:
    verify_surfaces:
      - D222
      - mint.quoteflow.baseline.lock
      - mint.no-legacy-coupling.guard
  agent_smoke_green:
    capability: mint.runtime.proof
    pass_pattern: "^status: OK"
