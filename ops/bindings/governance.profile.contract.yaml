---
status: authoritative
owner: "@ronny"
last_verified: 2026-03-28
scope: governance-profile-delivery
version: 1
updated_at: "2026-03-28"

source_packet: /Users/ronnyworks/code/.runtime/spine/state/domain-state/spine/execution-packets-20260326/UNIFIED_AGENT_WORKFLOW_BLOCKER_MODEL_PACKET_20260328.md

profiles:
  full_governance:
    description: "Live governed hook/attach context with dynamic runtime injection and enforced mutation discipline."
    entry_mechanism: governed_attach_or_live_hook_injection
    required_properties:
      - live governed hook/attach context
      - dynamic runtime injection
      - role/write-scope enforcement
      - receipts required for mutations
      - verification posture enforced
    restrictions: []
    current_lanes:
      - claude_code

  minimal_governance:
    description: "Governed attach first, then a thin authoritative adapter with bounded scope and on-demand reads of canonical docs/state."
    entry_mechanism: governed_attach_plus_thin_authoritative_adapter
    required_properties:
      - governed attach completed first
      - thin authoritative entry/adapter surface
      - on-demand reads of canonical docs
      - explicit controller prompt or bounded task scope
      - receipts required for mutations
    restrictions:
      - no claim of live hook parity
    current_lanes:
      - codex

  lightweight_degraded:
    description: "No full or minimal governance parity; read/draft only with truthful degraded posture."
    entry_mechanism: static_adapter_or_degraded_bridge_surface
    required_properties:
      - no full or minimal governance parity
      - read-only discovery
      - drafting
      - evidence rendering
      - handoff preparation
    restrictions:
      - no governed state mutation
      - no repo mutation
      - no parity claims
    current_lanes:
      - opencode
      - bridge_capable
      - offline

lane_assignments:
  claude_code: full_governance
  codex: minimal_governance
  opencode: lightweight_degraded
  bridge_capable: lightweight_degraded
  offline: lightweight_degraded

external_postures:
  cowork:
    posture: out_of_scope_until_governed_adapter_exists
    description: "Cowork is not a governance-profile lane in this pass."
    restrictions:
      - no governed repo mutation
      - no governed state mutation
      - no parity claims

resolution:
  default_profile_when_unresolved: full_governance
  default_lane_for_claude_hook: claude_code
  note: "Hook/adapter delivery may fall back to full_governance when contract resolution is unavailable."
