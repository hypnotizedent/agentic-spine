version: 1
domain: loop_gap
owner: '@ronny'
status: canonical
capability_membership:
  total_governed: 96
  catalog_domain_external: 0
  note: >
    All loop_gap capabilities are plane: fabric.
    The domain catalog lists only domain_external capabilities,
    so the catalog view for loop_gap is intentionally empty.
contract_bundle:
  policy:
    source_of_truth: docs/governance/domains/loop_gap.md
  verify:
    command: ./bin/ops cap run verify.run -- domain loop_gap
