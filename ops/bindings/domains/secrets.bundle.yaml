version: 1
domain: secrets
owner: '@ronny'
status: canonical
contract_bundle:
  policy:
    source_of_truth: docs/governance/domains/secrets.md
  verify:
    command: ./bin/ops cap run verify.run -- domain secrets
