# Device Map Manual Overrides
# Enriches ha.device.map.yaml with metadata for orphaned devices
# that cannot be matched automatically by MAC, hostname, or IEEE address.
# Keyed by ha_device_id. Merge happens in ha-device-map-build.
schema_version: 1
last_updated: "2026-03-22"

overrides:
  # media-home VM 106 — HA device exposes only a UniFi tracker entity and
  # falls through to a bad name_partial match without an explicit override.
  - ha_device_id: 0e65781007b464ea69785d41ee3b2dd7
    network_device_id: media-home
    match_method: manual_override
    location: vm-home
    category: media
    note: "Canonical home media plane (VM 106) via UniFi tracker device_tracker.unifi_default_bc_24_11_dc_5e_c1"

  # Residual media-service telemetry from the legacy shop plane. Keep visible,
  # but mark it explicitly non-canonical for incident triage.
  - ha_device_id: 517c1c950b43594d0f3a56a5092c9b67
    location: vm-shop
    category: media
    note: "Residual media-service telemetry from download-stack (192.168.1.209); canonical active media plane is media-home VM 106"

  # SABnzbd instances — VM-hosted Usenet clients, no MAC in device registry
  - ha_device_id: 9001382e8d895a993b0b1ecd780407cd
    location: vm-shop
    category: media
    note: "SABnzbd shop instance on VM 209"

  - ha_device_id: 9ebb00afe8e142fe4ae52cd946113a88
    location: vm-home
    category: media
    note: "SABnzbd home instance on VM 210"

  # Empress iPhone — false name_partial match to bedroom-empress-bulb (GAP-OP-578)
  - ha_device_id: 01e52f752723d210c5a74021ed406222
    network_device_id: null
    match_method: null
    note: "Empress is a companion app device (iPhone), not bedroom-empress-bulb Tuya light"

  # HA internal — Backup entity, not a physical device
  - ha_device_id: 9a4f25c4517b4e4da584c1cdd78d74a4
    location: ha-internal
    category: system
    note: "HA built-in backup entity"
