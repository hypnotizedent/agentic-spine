version: 1
domain: home-assistant
owner: '@ronny'
status: canonical
runtime_namespace:
  capability_prefixes:
    - ha.
    - ha-inventory-
  plugin_paths:
    - ops/plugins/domains/ha/
  binding_paths:
    - ops/bindings/domains/ha/
capability_membership:
  total_governed: 39
  catalog_domain_external: 38
  fabric_capabilities:
    - ha.identity.mutation.contract.status
contract_bundle:
  policy:
    source_of_truth: docs/governance/domains/home-assistant.md
  verify:
    command: ./bin/ops cap run verify.run -- domain home-assistant
