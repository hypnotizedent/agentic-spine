version: 1
domain: media
owner: '@ronny'
status: canonical
contract_bundle:
  policy:
    source_of_truth: docs/governance/domains/media.md
  verify:
    command: ./bin/ops cap run verify.run -- domain media
