version: 1
domain: infra
owner: '@ronny'
status: canonical
capability_membership:
  total_governed: 107
  catalog_domain_external: 0
  fabric_capabilities_note: >
    All 107 governed capabilities are plane: fabric.
    The domain catalog remains empty because it lists only domain_external capabilities.
  mailroom_operational_slice_note: >
    Infra governs mailroom.bridge.*, mailroom.task.*, mailroom.runtime.*,
    and mailroom.log.rotate. mailroom.scan and mailroom.outbox.retention
    remain loop_gap to preserve the live split inside mailroom.*.
contract_bundle:
  policy:
    source_of_truth: docs/governance/domains/infra.md
  verify:
    command: ./bin/ops cap run verify.run -- domain infra
