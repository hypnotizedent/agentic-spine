version: 1
domain: core
owner: '@ronny'
status: canonical
capability_membership:
  total_governed: 91
  catalog_domain_external: 2
  fabric_capabilities_note: >
    89 of 91 governed capabilities are plane: fabric.
    The domain catalog lists only the 2 domain_external capabilities
    (membrane.ingest, membrane.status). Legacy alias names
    translator.ingest / translator.status remain documented in
    ops/bindings/vocabulary.debt.inventory.yaml as compatibility prose;
    fresh terminals should learn membrane.* and not translator.*.
contract_bundle:
  policy:
    source_of_truth: docs/governance/domains/core.md
  verify:
    command: ./bin/ops cap run verify.run -- domain core
