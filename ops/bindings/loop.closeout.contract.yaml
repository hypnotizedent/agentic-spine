version: "1.0"
updated: "2026-02-28"
owner: "@ronny"
scope: loop-closeout-finalization

validation:
  require_acceptance_pass: true
  run_key_regex: "CAP-[0-9]{8}-[0-9]{6}__[A-Za-z0-9._-]+__R[A-Za-z0-9]+"
  min_required_run_keys: 1

fix_to_lock:
  p0_p1_severities:
    - critical
    - high
  require_regression_lock_id: true
  require_evidence: true

protected_lanes:
  loops:
    - LOOP-MAIL-ARCHIVER-MICROSOFT-DEEP-IMPORT-20260226
  loop_prefixes:
    - LOOP-MD1400-
  gaps:
    - GAP-OP-973

closeout_receipts:
  directory: docs/planning/loop-closeouts
  require_single_receipt_per_loop: true
