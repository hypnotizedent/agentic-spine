---
status: authoritative
owner: "@ronny"
last_verified: 2026-03-28
scope: spine-surface-metabolism-classification
version: 1
updated_at: 2026-03-28

metadata:
  generated_at: "2026-03-28T23:30:00Z"
  source_loop: LOOP-SPINE-CLOSURE-AND-AGENTIC-CORE-STABILIZATION-20260326
  wave_id: WAVE-20260328-03
  governance_authorities:
    - ops/bindings/governed.change.lifecycle.contract.yaml
    - ops/bindings/spine.self-governance.lifecycle.contract.yaml
    - ops/bindings/root.authority.contract.yaml
  classification_scale:
    - final
    - active_not_yet_machine_enforced
    - stale_superseded
    - never_activated
  scout_artifacts:
    - /Users/ronnyworks/code/.runtime/spine/state/domain-state/spine/execution-packets-20260326/SURFACE_METABOLISM_SCOUT_ENGINE_STATUS_20260328.md
    - /Users/ronnyworks/code/.runtime/spine/state/domain-state/spine/execution-packets-20260326/SURFACE_METABOLISM_SCOUT_GOVERNANCE_STATUS_20260328.md
    - /Users/ronnyworks/code/.runtime/spine/state/domain-state/spine/execution-packets-20260326/SURFACE_METABOLISM_SCOUT_ARCHIVE_STATUS_20260328.md
    - /Users/ronnyworks/code/.runtime/spine/state/domain-state/spine/execution-packets-20260326/SURFACE_METABOLISM_SCOUT_DOMAIN_STATUS_20260328.md

aggregate_counts:
  top_level_bindings: 262
  contract_yaml_files: 106
  non_contract_yaml_files: 156
  domain_bundle_yaml_files: 20
  domain_subdirectory_yaml_files: 151
  enforcement_posture:
    machine_enforced: 50
    runtime_consumed: 80
    semantic_only: 100
    inactive: 32
  binding_enforcement_percentage:
    machine_enforced: 19
    operationally_consumed: 50
    declarative_only: 38
    dormant: 12
  plugin_families:
    core: 24
    domains: 16
    infra: 16
    providers: 8
    total: 64
    utility_bucket_caveat: >
      Flat bin/ and lib/ directories within each category inflate the
      directory count. True logical plugin families number approximately
      50 when utility buckets are excluded.
  capabilities:
    total_registered: 808
    lifecycle_ready: 798
    lifecycle_experimental: 8
    lifecycle_deprecated: 1
    plane_fabric: 632
    plane_domain_external: 175
    domain_none_unclassified: 206
  gates:
    total_registered: 414
    active: 116
    retired: 298
    verify_gate_scripts: 41

areas:
  - area_id: engine_core
    scope: >
      Spine execution hot-path: orchestration, session, verify, evidence,
      loops, lifecycle, kernel/aof, agent, authority, and infra/mailroom-bridge.
    dominant_state: final
    mixed: false
    machine_enforcement_posture: >
      Verify gates enforce gate registry, topology, ring policy, failure
      classification, and self-governance lifecycle. Session and lifecycle
      enforce contracts at runtime by execution-time failure.
    engine_relevance: critical
    representative_surfaces:
      - ops/plugins/core/orchestration/
      - ops/plugins/core/session/
      - ops/plugins/core/verify/
      - ops/plugins/core/evidence/
      - ops/plugins/core/loops/
      - ops/plugins/core/lifecycle/
      - ops/plugins/core/kernel/aof/
      - ops/plugins/core/agent/
      - ops/plugins/core/authority/
      - ops/plugins/infra/mailroom-bridge/
    reason: >
      10 plugin areas form the execution hot-path. Worker, verify, wave,
      session, and control-cycle all depend on these. ~135 capabilities.
      Cross-referenced via direct script_path calls between plugins.
    recommended_next_action: none_needed

  - area_id: governance_core
    scope: >
      Binding contracts, verify gate infrastructure, governance docs,
      and the newly landed governed change lifecycle companion contract.
    dominant_state: active_not_yet_machine_enforced
    mixed: true
    machine_enforcement_posture: >
      ~50 of 262 bindings are machine-enforced by verify gates (~19%).
      ~80 more are runtime-consumed (~31%). ~100 are semantic-only (~38%).
      ~32 appear inactive (~12%). The companion contract has test coverage
      but no runtime verify gate yet.
    engine_relevance: high
    representative_surfaces:
      - ops/bindings/*.contract.yaml
      - ops/bindings/governed.change.lifecycle.contract.yaml
      - ops/bindings/spine.self-governance.lifecycle.contract.yaml
      - ops/plugins/core/verify/
      - docs/governance/SPINE.md
      - docs/governance/TRANSLATOR_AUTHORITY_DOCTRINE_V1.md
      - docs/governance/SESSION_PROTOCOL.md
      - ops/archive/pre-2026-04-01-spine/docs/governance/GOVERNED_TASK_ENVELOPE_SPEC.md
    reason: >
      Governance infrastructure is structurally healthy. Machine enforcement
      covers the verify and session hot-paths. The semantic-only tier is
      large (~100 bindings declare truth that nothing checks). NORTH_STAR.md
      is missing from disk. EXECUTION_NODE_SPEC.md is explicitly historical.
      AGENT_GOVERNANCE_BRIEF.md now reads as compatibility-only, with live
      agent/session authority anchored in SPINE.md and SESSION_PROTOCOL.md.
      The companion contract is landed but not yet gate-enforced.
    recommended_next_action: declared_but_unwired_contract_enforcement

  - area_id: awareness_delivery_and_operator_surfaces
    scope: >
      Tier 2 active-support plugins: ops, recovery, handoff, alerting,
      proposals, context, tenant, briefing. Plus kernel governance
      sub-areas: docs, surface, budget, policy, slo, snapshot, version.
    dominant_state: active_not_yet_machine_enforced
    mixed: false
    machine_enforcement_posture: >
      None of these areas are directly gate-enforced. They are consumed
      at runtime by session and control-cycle scripts. Briefing is
      consumed by the calendar-surface-audit verify gate.
    engine_relevance: medium
    representative_surfaces:
      - ops/plugins/core/ops/
      - ops/plugins/core/recovery/
      - ops/plugins/core/handoff/
      - ops/plugins/core/alerting/
      - ops/plugins/core/proposals/
      - ops/plugins/core/context/
      - ops/plugins/core/tenant/
      - ops/plugins/core/briefing/
      - ops/plugins/core/kernel/docs/
      - ops/plugins/core/kernel/surface/
    reason: >
      15 plugin areas provide active support to the engine without being
      on the execution hot-path. They are consumed at runtime but not
      gate-verified. ~50 capabilities total.
    recommended_next_action: none_needed

  - area_id: archive_and_legacy
    scope: >
      Archive zones (ops/archive, ops/bindings/archive, docs/reference/legacy),
      deprecation registry (ops/deprecations.yaml), stale residue in
      active tree, and archive mechanism completeness.
    dominant_state: active_not_yet_machine_enforced
    mixed: true
    machine_enforcement_posture: >
      Deprecation registry (6 block rules) is machine-enforced at entry.
      Archive zones are read-only by convention, not by gate. No archive
      command, manifest index, tombstone mechanism, or freshness gate exists.
    engine_relevance: low
    representative_surfaces:
      - ops/archive/
      - ops/bindings/archive/pre-consolidation/
      - docs/reference/legacy/
      - ops/deprecations.yaml
      - docs/governance/CAPABILITY_LAYER_VISIBILITY_CLASSIFICATION_20260326.md
    reason: >
      Archive coverage is partial. Binding pre-consolidation archive is
      complete (426 files). Legacy governance archive is healthy. Gap
      archive exists. But ~15-20 stale items remain in the active tree
      (docs/core, docs/reference/brain/_imported, docs/reference/mint,
      docs/contracts/mint, bin/cli). 160 capabilities are classified for
      removal (80 obsolete + 80 retire) with no automated archival path.
      CAPABILITY_LAYER_VISIBILITY_CLASSIFICATION_20260326.md is retained as
      a historical baseline for archive planning, not as a live authority
      surface. Archive mechanism is manual and convention-based.
    recommended_next_action: satellite_core_family_retirement_planning

  - area_id: domain_runtime_candidates
    scope: >
      Domain plugin directories (16), domain binding directories (13),
      domain bundles (20), and provider plugins (5). Extraction candidacy,
      capability registry domain assignment, and naming drift.
    dominant_state: active_not_yet_machine_enforced
    mixed: true
    machine_enforcement_posture: >
      Domain plugins consume the engine (capability registration, cap
      runner, worktree lifecycle) but are not themselves gate-enforced.
      No verify gate validates domain plugin correctness.
    engine_relevance: low
    representative_surfaces:
      - ops/plugins/domains/mint/
      - ops/plugins/domains/media/
      - ops/plugins/domains/ha/
      - ops/plugins/domains/communications/
      - ops/plugins/domains/calendar/
      - ops/plugins/providers/
      - ops/bindings/domains/
    reason: >
      ~291 domain-runtime capabilities across 16 plugin directories.
      3 Tier 1 extraction candidates (mint, media, home-assistant).
      4 naming drift issues (home-assistant/ha, tax-legal/taxlegal,
      domain:service, domain:infrastructure). 206 domain:none caps with
      ~145 needing assignment. 1 stale domain (maker), 2 never-activated
      (inbox-shield, example). 151 workbench-implemented capabilities.
    recommended_next_action: capability_reconciliation_against_live_surfaces

  - area_id: external_or_workbench_residue
    scope: >
      Workbench-implemented capabilities, provider integrations (cloudflare,
      microsoft, tailscale, github, gitea), cross-repo pointer stubs,
      and external service bindings.
    dominant_state: active_not_yet_machine_enforced
    mixed: true
    machine_enforcement_posture: >
      Provider capabilities are runtime-consumed. Workbench implementations
      are registered in capabilities.yaml with implementation_repo: workbench.
      Pointer stubs use spine_pointer_stub: true convention but no gate
      validates pointer integrity.
    engine_relevance: low
    representative_surfaces:
      - ops/plugins/providers/cloudflare/
      - ops/plugins/providers/microsoft/
      - ops/plugins/providers/tailscale/
      - ops/plugins/providers/github/
      - ops/plugins/providers/gitea/
      - docs/reference/pillars/finance/
    reason: >
      151 capabilities are implemented in workbench, not in this repo.
      54 provider capabilities across 5 provider plugins. Pointer stubs
      in docs/reference/pillars route to workbench but are not gate-enforced.
      This tier will be significantly affected by domain extraction.
    recommended_next_action: domain_extraction_readiness_assessment

elected_post_audit_next_action:
  action: capability_reconciliation_against_live_surfaces
  rationale: >
    The metabolism audit reveals that the engine-core is final and healthy,
    but the broader capability registry has significant reconciliation
    needs: 206 domain:none capabilities needing assignment, 4 naming drifts,
    3 missing registrations, 160 capabilities flagged for removal, 32
    inactive bindings, and ~100 semantic-only contracts. Capability
    reconciliation against the now-classified live surfaces is the highest
    leverage next step. It directly enables the extraction candidates
    identified by this audit (mint, media, home-assistant) and cleans
    the registry truth that all downstream work depends on.
  alternatives_considered:
    - satellite_core_family_retirement_planning
    - declared_but_unwired_contract_enforcement
    - domain_extraction_readiness_assessment
