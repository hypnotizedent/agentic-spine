schema_version: "1.0"
status: incubating
owner: "@ronny"
updated_at: "2026-03-24T00:00:00Z"
description: >
  Governed Task Envelope — single neutral governed container consumed and emitted by all clients
  (Claude, ChatGPT, shell, mobile). Wraps the entry packet + adapter output + governance
  constraints into one dispatched unit. Response envelope is validated against original
  constraints by the verifier before attestation is issued.
contract: governed.task.envelope
version: "1.0"
spec: docs/governance/GOVERNED_TASK_ENVELOPE_SPEC.md
implementation_repo: workbench
implementation_path: agents/task-envelope/
relationship_to_entry_packet: wraps_entry_packet_does_not_replace_it
authority: declares_granted_authority_does_not_grant_it
