registry: HOME_AUTOMATION_REGISTRY_V1
version: "1.0"
status: pending
created: "2026-03-02"
authority: HOME_AUTHORITY_CONTRACT_V1
note: "Populate on-site during LOOP-HOME-CANONICAL-REALIGNMENT W3"

automations: []
  # To be populated from HA /api/config/automation/config
  # Format: { id, alias, description, trigger_type, enabled, last_triggered }

scenes: []
  # To be populated from HA /api/states (domain=scene)
  # Format: { entity_id, friendly_name, last_activated }

scripts: []
  # To be populated from HA /api/config/script/config
  # Format: { id, alias, description, mode, last_triggered }

helpers: []
  # To be populated from HA /api/states (input_* domains)
  # Includes: input_boolean, input_number, input_select, input_text, input_datetime
  # Format: { entity_id, type, friendly_name, state }
