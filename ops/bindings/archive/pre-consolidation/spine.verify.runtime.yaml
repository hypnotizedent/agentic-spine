version: 2
status: stable
mode: core_only
reason: "Permanent default — core-8 gates for daily work. Full suite via verify.release.run only."
owner: "@ronny"
changed_at: "2026-02-22"
expires_at: ""
full_verify_capability: "verify.release.run"
description:
  - "core_only is the permanent daily default (8 gates, <60s, no network calls)."
  - "Full 148-gate suite runs only via: ./bin/ops cap run verify.release.run"
  - "Network gates have Tailscale guards — skip cleanly when VPN is disconnected."
  - "To temporarily restore full mode: set mode=full with an expires_at date."
