schema_version: "1.0"
status: incubating
owner: "@ronny"
updated_at: "2026-03-24T00:00:00Z"
description: >
  Model Adapter Layer — provider-agnostic rendering surface that translates a neutral intermediate
  form (intent, context_pack, tool_affordances, autonomy_level, mutation_policy) into the
  native input format expected by a specific model provider. V1 scope is Claude adapter only.
contract: model.adapter
version: "1.0"
spec: ops/archive/pre-2026-04-01-spine/docs/governance/MODEL_ADAPTER_LAYER_SPEC.md
implementation_repo: workbench
implementation_path: agents/model-adapter/
v1_scope: claude_adapter_only
integration_point: between_context_bundle_compiler_and_task_envelope
authority: rendering_only_no_execution_no_state_no_governance_bypass
