version: "2.0"
updated: "2026-04-15"
owner: "@operator"
scope: entry-boot-surface-projection
status: authoritative

boot_model: injected
injection_surfaces:
  codex:
    mechanism: governed-attach
    path: ops/plugins/core/lifecycle/bin/session-v3-attach
  opencode:
    mechanism: governed-attach
    path: ops/plugins/core/lifecycle/bin/session-v3-attach
  claude:
    mechanism: convention-read
    entry_file: CLAUDE.md
    note: Claude Code reads repo-root CLAUDE.md by convention; no injection hook available

surfaces:
  - name: AGENTS.md
    role: source
    note: canonical agent entry surface; single source of aperture and authority pointers
  - name: CLAUDE.md
    role: projection
    source: AGENTS.md
    note: redirect surface for Claude Code repo-root convention

projections:
  home_codex_agents:
    path: ~/.codex/AGENTS.md
    role: projection
    source: AGENTS.md
    note: home-level copy; repo AGENTS.md is authoritative, home copy must track it

generated_file: docs/reference/generated/BOOT_ENTRY_SURFACE.md
