version: "2.0"
updated: "2026-03-27"
owner: "@ronny"
scope: entry-boot-surface-projection
status: authoritative

# Post H2-C: stubs are thin pointers. Boot/governance injection is handled
# by agent-specific mechanisms, not by inline blocks in entry stubs.

boot_model: injected
injection_surfaces:
  claude_code:
    mechanism: hook
    path: .claude/hooks/session-entry-hook.sh
    trigger: UserPromptSubmit
  codex:
    mechanism: governed-attach
    path: ops/plugins/core/session/bin/session-v3-attach
  opencode:
    mechanism: governed-attach
    path: ops/plugins/core/session/bin/session-v3-attach

# Stubs listed here are pointer-only surfaces. They must NOT carry
# inline startup blocks, verify commands, or certification commands.
surfaces:
  - AGENTS.md
  - CLAUDE.md

generated_file: docs/reference/generated/BOOT_ENTRY_SURFACE.md
