status: authoritative
owner: "@ronny"
last_verified: "2026-03-01"
scope: friction-baseline-contract
version: "1.0"
updated_at: "2026-03-01"

artifact:
  path: "mailroom/state/friction-baseline.yaml"
  checksum_algorithm: "sha256"
  checksum_field: "checksum_sha256"

required_fields:
  - version
  - contract_id
  - generated_at_utc
  - timezone_human
  - loop_id
  - commands
  - baseline_failing_gate_ids
  - checksum_sha256

required_commands:
  - "./bin/ops cap run verify.run -- fast"
  - "./bin/ops cap run verify.pack.run loop_gap"
  - "./bin/ops cap run verify.route.recommend"

timezone_human_default: "America/New_York"
