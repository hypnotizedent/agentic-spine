#!/usr/bin/env bash
set -euo pipefail

# Deprecated dispatcher shim.
# Canonical entrypoint is bin/ops.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
echo "DEPRECATED: ops/ops is a deprecated shim. Use bin/ops instead." >&2
exec "$SCRIPT_DIR/../bin/ops" "$@"
