#!/usr/bin/env bash
set -euo pipefail

ROOT="${SPINE_ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../../../.." && pwd)}"

"$ROOT/surfaces/verify/d127-domain-assignment-drift-lock.sh"

echo "gate.topology.validate PASS: topology contract and domain assignments are valid"
