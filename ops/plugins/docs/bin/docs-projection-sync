#!/usr/bin/env bash
set -euo pipefail

ROOT="${SPINE_CODE:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../../../.." && pwd)}"

"$ROOT/bin/generators/gen-entry-surface-gate-metadata.sh" --write-surfaces
"$ROOT/bin/generators/gen-boot-entry-surface.sh" --write-surfaces

echo "docs.projection.sync"
echo "status: OK"
