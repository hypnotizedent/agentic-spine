#!/usr/bin/env bash
set -euo pipefail

ROOT="${SPINE_CODE:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../../../.." && pwd)}"

"$ROOT/bin/generators/gen-entry-surface-gate-metadata.sh" --check
"$ROOT/bin/generators/gen-boot-entry-surface.sh" --check

echo "docs.projection.verify"
echo "status: OK"
