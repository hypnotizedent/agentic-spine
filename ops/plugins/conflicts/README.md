# Conflicts Plugin

This folder holds artifacts used by the conflicts-aware plugin. The plugin
will eventually search repositories for merge separators and report them as
tasks. For now, we keep a template and a stub script for future wiring.
