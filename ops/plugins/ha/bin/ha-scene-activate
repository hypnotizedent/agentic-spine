#!/usr/bin/env bash
set -euo pipefail
# ha.scene.activate — Activate a Home Assistant scene (idempotent preset)
SPINE_ROOT="${SPINE_ROOT:-$HOME/code/agentic-spine}"
exec "$SPINE_ROOT/ops/plugins/ha/bin/ha-service-call" --domain scene --service turn_on --entity "${1:?entity_id required}"
