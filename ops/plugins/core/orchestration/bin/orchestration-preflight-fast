#!/usr/bin/env bash
# orchestration-preflight-fast - Fast non-blocking preflight (<=120s target)
# Called by: ops cap run orchestration.preflight.fast <domain>
set -euo pipefail

SPINE_REPO="${SPINE_REPO:-$HOME/code/agentic-spine}"
exec "$SPINE_REPO/ops/commands/wave.sh" preflight "$@"
