#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../../../.." && pwd)"
exec python3 "$ROOT/ops/plugins/core/authority/bin/_l2_readback.py" token.custody.status "$@"
