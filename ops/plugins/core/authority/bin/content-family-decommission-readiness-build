#!/usr/bin/env python3
from __future__ import annotations

import argparse
import os
import subprocess
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[5]
if str(ROOT) not in sys.path:
    sys.path.insert(0, str(ROOT))

from ops.plugins.infra.lib.content_family_decommission_readiness import (
    build_projection,
    render_markdown,
    source_paths,
)
from ops.plugins.infra.lib.content_family_placement import (
    load_yaml,
    rel,
    source_sha,
    source_stamp,
    yaml_dump,
)


def expand(raw: str | Path) -> Path:
    return Path(os.path.expandvars(os.path.expanduser(str(raw)))).resolve()


DEFAULT_ROOT = expand(ROOT)


def git_top_level(start: Path) -> Path | None:
    probe = start if start.is_dir() else start.parent
    proc = subprocess.run(
        ["git", "-C", str(probe), "rev-parse", "--show-toplevel"],
        text=True,
        capture_output=True,
    )
    if proc.returncode != 0:
        return None
    stdout = proc.stdout.strip()
    return Path(stdout).resolve() if stdout else None


def resolve_target_root(explicit: str | None = None) -> Path:
    if explicit:
        return expand(explicit)
    cwd_root = git_top_level(Path.cwd())
    if cwd_root is not None:
        return cwd_root
    script_root = git_top_level(Path(__file__).resolve())
    if script_root is not None:
        return script_root
    for env_name in ("SPINE_TARGET_REPO", "SPINE_ROOT", "SPINE_REPO", "SPINE_CODE"):
        value = os.environ.get(env_name)
        if value:
            return expand(value)
    return DEFAULT_ROOT


def fail(message: str, *, exit_code: int = 1) -> None:
    print(f"content.family.decommission.readiness.build FAIL: {message}", file=sys.stderr)
    raise SystemExit(exit_code)


def parse_args() -> argparse.Namespace:
    parser = argparse.ArgumentParser(prog="content-family-decommission-readiness-build")
    parser.add_argument("--root", help="Target checkout root.")
    parser.add_argument("--check", action="store_true", help="Fail if generated projection or matrix drifts from disk.")
    parser.add_argument("--verify", action="store_true", help="Verify source authority files exist.")
    parser.add_argument("--stdout", action="store_true", help="Print the rendered YAML projection instead of writing files.")
    return parser.parse_args()


def main() -> None:
    args = parse_args()
    root = resolve_target_root(args.root)
    policy_path = Path(os.environ.get("CONTENT_FAMILY_PLACEMENT_POLICY", Path("/Users/ronnyworks/code/projects/media/bindings/content.family.placement.policy.yaml")))
    projection_path = Path(os.environ.get("CONTENT_FAMILY_DECOMMISSION_READINESS_PROJECTION", Path("/Users/ronnyworks/code/projects/media/bindings/content.family.decommission.readiness.projected.yaml")))
    matrix_path = root / "docs/reference/generated/CONTENT_FAMILY_DECOMMISSION_READINESS.md"
    all_sources = source_paths(root)

    try:
        policy = load_yaml(policy_path)
    except Exception as exc:
        fail(f"unable to load family placement authority: {exc}")

    if str(policy.get("authority_state") or "") != "authoritative":
        fail("content family placement authority missing authority_state=authoritative")

    if args.verify:
        for path in all_sources:
            if not path.exists():
                fail(f"missing required source: {rel(root, path)}")

    projection = build_projection(
        root,
        policy,
        source_sha256=source_sha(root, all_sources),
        generated_at_utc=source_stamp(root, all_sources),
    )
    projection_header = "\n".join(
        [
            "# Content Family Decommission Readiness Projection",
            "# Non-secret. Spine-native.",
            "# Status: projection",
            "# projection_of: /Users/ronnyworks/code/projects/media/bindings/content.family.placement.policy.yaml",
            "# Do NOT edit manually. Rebuild with: ./bin/ops cap run content.family.decommission.readiness.build",
            "",
        ]
    )
    rendered_yaml = projection_header + yaml_dump(projection)
    rendered_md = render_markdown(projection)

    if args.stdout:
        print(rendered_yaml, end="")
        return

    if args.check:
        current_yaml = projection_path.read_text(encoding="utf-8") if projection_path.exists() else ""
        current_md = matrix_path.read_text(encoding="utf-8") if matrix_path.exists() else ""
        if current_yaml != rendered_yaml or current_md != rendered_md:
            fail("projection or readiness matrix drift detected")
        print(
            "content.family.decommission.readiness.build PASS: "
            f"{rel(root, projection_path)} and {rel(root, matrix_path)} match authority"
        )
        return

    projection_path.write_text(rendered_yaml, encoding="utf-8")
    matrix_path.parent.mkdir(parents=True, exist_ok=True)
    matrix_path.write_text(rendered_md, encoding="utf-8")
    print(
        "content.family.decommission.readiness.build PASS: "
        f"wrote {rel(root, projection_path)} and {rel(root, matrix_path)}"
    )


if __name__ == "__main__":
    main()
