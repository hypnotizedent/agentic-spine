#!/usr/bin/env bash
set -euo pipefail

ROOT="${SPINE_CODE:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../../../../.." && pwd)}"

"$ROOT/ops/plugins/core/ops/bin/gen-entry-surface-gate-metadata.sh" --check
"$ROOT/ops/plugins/core/ops/bin/gen-boot-entry-surface.sh" --check
bash "$ROOT/ops/plugins/core/authority/bin/authority-concerns-projection-build" --check
python3 "$ROOT/ops/plugins/core/authority/bin/service-registry-projection-build" --check --verify

echo "docs.projection.verify"
echo "status: OK"
