#!/usr/bin/env bash
# Thin exec shim — canonical implementation lives in workbench
# See: agents/immich/spine-plugin-immich/bin/immich-reconcile-plan
set -euo pipefail
export SPINE_ROOT="${SPINE_ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../../../../.." && pwd)}"
exec "$HOME/code/workbench/agents/immich/spine-plugin-immich/bin/immich-reconcile-plan" "$@"
