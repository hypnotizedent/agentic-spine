#!/usr/bin/env bash
# Example domain health check capability
# Register in ops/capabilities.yaml to use via: ./bin/ops cap run example.health.check
set -euo pipefail

echo "example.health.check"
echo "status: healthy"
echo "domain: example"
echo "note: Replace this with your domain-specific health checks"
