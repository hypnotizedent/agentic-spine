---
schema_version: "1.0"
status: template
owner: "@your-handle"
scope: example-domain

contract: example.domain
version: "1.0"

description: >
  Template domain contract. Replace with your domain-specific
  authority boundaries, endpoints, and dependency declarations.

authority_boundary:
  reads:
    - "example service API"
  writes: []
  executes: []

dependencies: []
