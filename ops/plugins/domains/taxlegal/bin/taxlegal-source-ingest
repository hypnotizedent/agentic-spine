#!/usr/bin/env bash
set -euo pipefail
# taxlegal.source.ingest — stub
# Registered in capabilities.yaml (lifecycle: experimental).
# Implementation pending L3 domain extraction.
echo "ERROR: taxlegal.source.ingest is not yet implemented (lifecycle: experimental)" >&2
exit 1
