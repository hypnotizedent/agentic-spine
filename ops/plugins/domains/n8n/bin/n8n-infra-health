#!/usr/bin/env bash
# Thin exec shim — canonical implementation lives in workbench
# See: agents/n8n/spine-plugin-n8n/bin/n8n-infra-health
set -euo pipefail
export SPINE_ROOT="${SPINE_ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../../../../.." && pwd)}"
exec "$HOME/code/workbench/agents/n8n/spine-plugin-n8n/bin/n8n-infra-health" "$@"
