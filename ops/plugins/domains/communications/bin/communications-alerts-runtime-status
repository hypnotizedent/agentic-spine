#!/usr/bin/env bash
set -euo pipefail
# Thin shim — delegates to communications project canonical implementation.
# Product truth lives at projects/communications/tools/spine-plugin-communications/
CODE_ROOT="${SPINE_CODE_ROOT:-${HOME}/code}"
exec "$CODE_ROOT/projects/communications/tools/spine-plugin-communications/bin/communications-alerts-runtime-status" "$@"
