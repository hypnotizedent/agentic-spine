#!/usr/bin/env bash
# Thin shim — canonical source is now in workbench.
export SPINE_ROOT="${SPINE_ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../../../../.." && pwd)}"
exec "${COMMUNICATIONS_TARGET_REPO:-${SPINE_FOUNDATION_ROOT:-/Users/ronnyworks/code/workbench}/agents/communications/tools/spine-plugin-communications}/bin/communications-alerts-runtime-status" "$@"
