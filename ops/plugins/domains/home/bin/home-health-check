#!/usr/bin/env bash
# home-health-check — thin shim; canonical copy lives in workbench
exec "${WORKBENCH_ROOT:-$HOME/code/workbench}/agents/home-assistant/tools/spine-plugin-home/bin/home-health-check" "$@"
