#!/usr/bin/env bash
set -euo pipefail
# ha.light.toggle — Toggle a Home Assistant light entity
SPINE_ROOT="${SPINE_ROOT:-$HOME/code/agentic-spine}"
exec "$SPINE_ROOT/ops/plugins/domains/ha/bin/ha-service-call" --domain light --service toggle --entity "${1:?entity_id required}"
