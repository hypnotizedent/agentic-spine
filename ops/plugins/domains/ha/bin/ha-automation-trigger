#!/usr/bin/env bash
set -euo pipefail
# ha.automation.trigger — Trigger a Home Assistant automation
SPINE_ROOT="${SPINE_ROOT:-$HOME/code/agentic-spine}"
exec "$SPINE_ROOT/ops/plugins/domains/ha/bin/ha-service-call" --domain automation --service trigger --entity "${1:?entity_id required}"
