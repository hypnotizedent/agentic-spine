#!/usr/bin/env bash
# Thin compatibility shim — authority lives in mint-modules.
# See: mint-modules/scripts/ops/customer-reply-draft.py
MINT_MODULES_ROOT="${MINT_MODULES_ROOT:-$HOME/code/mint-modules}"
exec "$MINT_MODULES_ROOT/scripts/ops/customer-reply-draft.py" "$@"
