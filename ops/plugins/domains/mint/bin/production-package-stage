#!/usr/bin/env bash
# Thin compatibility shim — authority lives in mint-modules.
# See: mint-modules/scripts/ops/production-package-stage.sh
MINT_MODULES_ROOT="${MINT_MODULES_ROOT:-$HOME/code/mint-modules}"
exec "$MINT_MODULES_ROOT/scripts/ops/production-package-stage.sh" "$@"
