#!/usr/bin/env bash
# production-package-show - Wrapper for read-only Mint production package inspection

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
LIB_DIR="$SCRIPT_DIR/../lib"

exec python3 "$LIB_DIR/production_package_show.py" "$@"
