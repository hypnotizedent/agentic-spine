#!/usr/bin/env bash
# production-package-stage - Wrapper for governed Mint production package staging

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
LIB_DIR="$SCRIPT_DIR/../lib"

exec python3 "$LIB_DIR/production_package_stage.py" "$@"
